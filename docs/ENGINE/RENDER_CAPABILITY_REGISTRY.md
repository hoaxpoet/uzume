# Render Capability Registry

**Status legend**

- **Supported** — capability exists in the renderer, exercised by at least one shipping preset, and covered by tests.
- **Partial** — capability exists for one path or one preset family but is not generalised across the renderer; using it from a new preset family requires non-trivial wiring.
- **Missing** — capability does not exist in the codebase. Adding it is a renderer change, not a preset change.
- **Unknown** — capability has been discussed but not investigated; status to be confirmed before any preset depends on it.

This registry was first compiled from the Arachne v8 architecture audit (`docs/VISUAL_REFERENCES/arachne/Arachne_Rendering_Architecture_Contract.md`, **deleted at D-246 with the preset — recover from git history**). It is the engine-side source of truth for "what we can already render" and the gating document for new preset families.

Entries should cite source files. When adding a capability, link to the files that implement it. When marking a capability missing, link to the closest related code so future work has a starting point.

---

## 1. Composition capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Drawable lifecycle diagnostics across every presented render path | Supported (HANG.1 instrumentation; HANG.2 live control) | `DrawableLifecycleProbe.swift`; instrumented acquisition/presentation wrappers in every `RenderPipeline+*.swift` drawable path; `VisualizerEngine+InitHelpers.setupDrawableLifecycleWatchdog`; `DrawableLifecycleProbeTests`; `docs/diagnostics/BUG085_HANG2_SOAK_2026-08-05.md` | Correlates unique drawable objects with scheduled presentation and command-buffer completion. Writes a balanced heartbeat every 600 completions, immediate failure/unpresented alarms, and the pending call site after a drawable request blocks for 500 ms. HANG.2 held a 34,811/34,811 acquired/presented balance through a 10 min 36 s Witchlight soak and two track transitions, with zero stalls or imbalances. Observation only: BUG-085 remains open and intermittent; no fix is justified without a frozen capture. |
| Direct fragment to drawable | Supported | `RenderPipeline+Draw.swift` (drawDirect path); all 2D presets | Default rendering path. |
| Render-to-offscreen-texture (single target) | Supported | `RenderPipeline+MVWarp.swift:325` ("Render the preset's direct fragment shader to an offscreen texture"); `RenderPipeline+RayMarch.swift:90–95` (`if let offscreen = sceneOutputTexture`) | Used by mv_warp warp grid and by ray-march for the warp-target case. |
| Multipass orchestration with named per-pass outputs (per-preset DAG) | Supported (linear) | V.ENGINE.1: `RenderPass.staged` + `PresetDescriptor.stages: [PresetStage]` + `RenderPipeline+Staged.swift`. Diagnostic preset `StagedSandbox` declares `["world", "composite"]` and ships green in `StagedCompositionTests`. | Linear-only DAG (later stages may sample any earlier stage; no fan-out / branching graph). Sufficient for Arachne v8's WORLD → BG_WEBS → FG_WEB → DROPLETS → COMPOSITE chain. A general DAG is deferred. |
| Half-resolution offscreen targets | **Missing** | The only half-res target was SSGI's, deleted at RECON.18 | No half-res offscreen path today; a preset needing one allocates its own. |
| Stage state that survives across frames (ping-pong pair per stage) | Supported | ALFVEN.1 (D-244): `"persistent": true` on a stage. `setStagedRuntime` allocates a zeroed pair in the stage's declared format; each frame the stage samples the previous frame's half at `[[texture(20)]]` (`kStagedPersistentTextureSlot`) and renders the other, then they swap. Zeroed at allocation, on preset switch, and by `resetStagedPersistentState()`. Tested in `StagedPersistenceTests` (frame 2 reads frame 1: 1 → 2 exactly) and `PersistentStagedPathHarnessTemplate`. | A persistent FINAL (drawable-writing) stage is a decode error — the view owns the drawable. Persistent textures are `.shared` (UMA) so the watchdog can probe them without a blit. No cross-preset carry-over: switching presets always zeroes. |
| Stage that runs N times per frame against its own output (relaxation solver) | Supported | ALFVEN.1 (D-244): `"iterations": N`, range 1…64. The stage encodes N render passes per frame, ping-ponging its own pair, with its `samples` inputs held CONSTANT across all N. Composes with `persistent` — iteration 1 of frame N+1 warm-starts from frame N. Lives in the existing stage loop (`encodeOffscreenStages`), not a parallel path. Tested in `StagedPersistenceTests` (`iterations: 8` ⇒ exactly +8/frame, +16 after two frames when persistent). | Cost is linear in N: N full-screen passes per frame. 24 sweeps of Jacobi at 256² is a PARTIAL solve — it removes ~11 % of the domain-scale error per frame and relies on `persistent` to converge over ~10 s. Sizing an iteration count without measuring convergence is how a solver silently ships unconverged. |
| Per-stage offscreen pixel format | Supported | ALFVEN.1 (D-244): `"pixel_format"` on a stage — allowlist `rgba16Float` (default, unchanged for every pre-existing sidecar), `rgba32Float`, `rg32Float`. `PresetLoader` compiles the stage's pipeline against it and `setStagedRuntime` allocates to match. Unknown values warn and fall back to `rgba16Float` (the `feedback_pixel_format` precedent, PUB.4). | Ignored on the final stage, which always takes the drawable format. `rgba32Float` costs 2× the bandwidth of `rgba16Float` — declare it for solver state, not for colour. |
| Non-finite watchdog on persistent GPU state | Supported | ALFVEN.1 (D-244), `ALFVEN_DESIGN.md` §8.3: `RenderPipeline.probeStagedPersistentState()` runs at the top of every staged frame, reads ONE rotating 16×16 texel block per persistent stage from `.shared` memory, and re-zeroes the pair on any NaN/Inf. Measured cost ~0.2 µs/frame Release (~114 µs Debug/-Onone, which is the finiteness scan, not the read). Tested by injecting a NaN and asserting the next frame is finite. | Detection is sparse by design — full coverage takes `(w/16)·(h/16)` frames (~1.4 s at 1080p/60). A blow-up confined to a few texels for many seconds could be missed; a Laplacian-coupled field spreads one within a frame or two, which is the case this guards. Recovery is a re-zero, not a re-seed: a preset that needs a specific initial state must re-seed itself. |
| Pseudo-spectral PDE solver on a compute pipeline (GPU FFT, adaptive dt, real substeps) | Supported | ALFVEN.4/.4b: `AlfvenSolver` (`ParticleGeometry`, D-097). Threadgroup-memory Cooley-Tukey FFT rows/cols; two real fields ride as the real/imaginary halves of ONE complex transform (Hermitian unpack). Spectral Poisson, spectral gradients, Poisson brackets, 2/3 dealiasing on the bracket product, Hou-Li filter on the state, and integrating-factor Heun (RK2). `dt` comes from a field-wide atomic-max CFL reduction MID-FRAME. Gated by `AlfvenComputeFFTTests` (round-trip 8.9e-07, Parseval 1.000000) and `AlfvenSolverTests`, which checks the clamp is never doing the work. | **The staged fragment path cannot host this** — it has no mid-frame reduction (so no adaptive dt) and no real substeps; that is why ALFVEN.4 moved off it. Every constant is PORTED from `docs/presets/alfven_spike/alfven.py`, which is RUNNABLE (numpy+scipy, ~40 s) and is the oracle for behavioural questions, not just API ones — run it before theorising. Two constants are DERIVED from the grid rather than copied (`nu4`, `spectralCutoff`): k⁴ hyperdiffusion and the Hou-Li filter are violently resolution-dependent. ⚠ Forward Euler is not viable here: `|1+iy| = sqrt(1+y²)` grows O(y²) at the top of the spectrum and renders as grid-scale hatching on the steepest ridges. |
| Sample-previously-rendered-pass texture from a later fragment in the same frame | Supported | V.ENGINE.1: stage `samples: [...]` array binds earlier stage textures starting at `[[texture(13)]]` (`kStagedSampledTextureFirstSlot`). Tested end-to-end in `StagedCompositionTests.stagedSandboxRendersAndSamplesEarlierStage`. | Slot range: 13+. Slots 0–12 remain reserved for FFT/wave/stems/scene/spectral-history (buffers) and noise/IBL/text (textures). |
| Per-preset multipass shaders (e.g. WORLD → COMPOSITE) | Supported | V.ENGINE.1: `Sources/Renderer/RenderPipeline+Staged.swift`; preset declares `passes: ["staged"]` + `stages: [...]` and the renderer compiles one fragment pipeline per stage. `StagedSandbox` is the diagnostic. | Bridges via `StagedStageSpec` in Renderer / `LoadedStage` in Presets / `setStagedRuntime(_:drawableSize:)` on `RenderPipeline`. |
| Depth attachments / explicit depth buffer | Missing | The ray-march G-buffer depth attachment existed only for the mesh G-buffer path and was deleted with it at RECON.15; the SDF path never attached it and direct-fragment passes have none | No depth available to atmospheric / DoF effects. A future mesh/raster preset re-adds a depth target with its pass. |
| Compositing a 3D ray-march scene under a 2D direct fragment overlay | Missing | — | Not currently a documented combination. |
| ACES tone-map composite | Supported | `PostProcessChain` + `Shaders/PostProcess.metal` (CLAUDE.md §PostProcessChain). Composite always runs even when bloom is gated by the budget governor. | Standard tail of the post-process pass. |
| Bloom (bright-pass + ping-pong blur) | Supported | `PostProcessChain` (gated by `bloomEnabled`) | Gated off by `FrameBudgetManager` at quality `.noBloom` and below. |

## 2. Geometry capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Fullscreen quad / direct fragment SDF | Supported | `Common.metal` `fullscreen_vertex`; every 2D preset | The default 2D path. |
| 2D SDF utility library (primitives, boolean, modifiers, displacement, hex tile, ray march) | Supported | `Sources/Presets/Shaders/Utilities/Geometry/` (V.2, see CLAUDE.md §Module Map) | 30 `sd_*` primitives + booleans + modifiers + adaptive ray march + hex tile. |
| 3D SDF ray march (deferred G-buffer + lighting) | Supported | `RayMarchPipeline.swift`, `RayMarchPipeline+Passes.swift`, `Shaders/RayMarch.metal` | Cook-Torrance PBR deferred lighting. Used by Volumetric Lithograph, Test Sphere. (Kinetic Sculpture retired KSRETIRE.1 / D-188.) |
| Mesh-rendered G-buffer path for ray-march presets (`MeshGBufferEncode`) | **DELETED — RECON.15 (2026-08-26)** | Was `RayMarchPipeline.meshGBufferEncoder` + `setMeshGBufferEncoder` + `runMeshGBufferPass` + the G-buffer depth attachment, letting a preset emit raster geometry into the deferred G-buffer instead of marching an SDF. Sole implementation `FerrofluidMesh` (.swift + .metal) was unwired at Ferrofluid Ocean round 57 (2026-05-17, "scoop" normal artifacts) and never re-enabled; production only ever called `setMeshGBufferEncoder(nil)`, and the test hook (`useMeshPath`) had no caller passing `true`. Applies the D-203/D-213 precedent: good work is not a reason to keep code with no consumer. | A future preset wanting PBR lighting on raster geometry rebuilds this dispatch (git history: `FerrofluidMesh`). The shelved Goldengrove plan named it as its architecture — see `docs/presets/GOLDENGROVE_PLAN.md` §2, annotated. Ferrofluid Ocean's live SDF path and its baked height texture (row below) are unaffected. |
| GPU procedural particle system (compute + render, UMA buffer) | **DELETED — RECON.17 (2026-08-26)** | Was `Sources/Renderer/Geometry/ProceduralGeometry.swift` + `Shaders/Particles.metal` (the 2D Murmuration flock: `Particle` struct, `ParticleConfig`, `particle_update`, `particle_vertex`/`particle_fragment`). Murmuration moved to `Murmuration3DGeometry` (row below, certified) and nothing else ever adopted the generic path — it was constructed only by `ProceduralGeometryTests` and `MurmurationStemRoutingTests`, which validated stem routing of a kernel no preset ran; both deleted with it. **This row's own retirement condition** — *"retiring it is a later cleanup once nothing else consumes it"* — was met, and `MURMURATION_DESIGN.md` §12 anticipated it explicitly. Its value as the MM.3 audio-coupling reference source lives in git history. | New particle presets write their own `ParticleGeometry` conformer (D-097: siblings, not subclasses) — that was already the rule, and every conformer since has followed it. |
| GPU 3D parametric-ellipse flock (controlled, audio-coupled, **certified**) | Supported | `Sources/Renderer/Geometry/Murmuration3DGeometry.swift`; `Shaders/Murmuration3D.metal` | Murmuration's **shipped + certified** flock (D-097 `ParticleGeometry` sibling). **MM.6 3D rebuild** lifting the proven 40-round 2D controlled-ellipse flock (`Particles.metal`) into 3D after the emergent Flock2 port failed M7 ×7 (FA #73; design §13): ~14 K birds spring-pulled (`3·d+5·d²`, damping `1−3·dt`) to home slots in a continuously morphing 3D ellipsoid; perspective + depth fade; **wheeling comma** (centred C↔S, no spine wave) + **internal churn** (smooth-in-(u,v,w) flow → the boil) + **continuous rolling dark bands**; banking darkening = wing-area-to-camera. Dense + framed **by construction** (the property pure emergence couldn't guarantee). **Audio coupling = GLOBAL ENVELOPE** (design §13.5, smoothed CPU-side in `advanceEnvelopes`, passed via 48 B `M3DConfig` MSL-mirror): `energyEnv` → vigor-paced morph clock + swell/elongation + traverse range (PRIMARY, D-026); `beatEnv` → beat-gated agitation/banking wave (ACCENT); `vocalEnv` → density. `activeParticleFraction`-governed (throttles **sub-steps**, never bird count — coupled-flock rule). Verified via `Murmuration3DRenderTests` (framed-across-traverse + musicality + contact sheets, production dispatch path). **CERTIFIED MM.5 (2026-06-04, `8f313bdc`).** **Retired** the emergent Flock2 substrate (`MurmurationFlock*` + `MurmurationFlockGeometry`, `git rm`'d 9056dc48) and, before it, the MM.2/MM.3 force-boids. |
| Mesh shaders on M2+ (with vertex fallback below) | Supported | `Sources/Renderer/Geometry/MeshGenerator.swift`; `Shaders/MeshShaders.metal` | Used by Fractal Tree (Stalker retired). `densityMultiplier` is governor-gated. **Title corrected FTR.1:** the dispatch gate is `device.supportsFamily(.apple8)` = **M2 and later**, not M3 — the doc-comment in `MeshGenerator.swift` still says "M3+" and is stale. **BLOCKER CLEARED (FTR.4, 2026-08-11): `StemFeatures` is now bound at OBJECT and MESH buffer(3)** by `MeshGenerator.draw(encoder:features:stems:)`, the slot the fragment stage and every non-mesh path already used; `drawWithMeshShader` passes the same value it was already binding to the fragment stage. Every mesh preset inherits it — a mesh preset can now SHAPE by stem, not only colour by stem. Gated on CONSUMPTION rather than binding (`objectStageReceivesStems`): the same FeatureVector rendered twice, changing only `other_onset_rate`, must produce different pixels and more of them for the busier guitar. First consumer: Fractal Tree's tips (FTR.8). **FTR.13 adds a THIRD: the beat-held `StemFeatures` at OBJECT/MESH buffer(5)**, and makes the hold EASED rather than snapped. `BeatHold(easeBeats:)` is a new opt-in initialiser — `BeatHold()` keeps the FTR.10 hard snap and its tests unchanged — under which the snapshot starts moving ON the beat and arrives `easeBeats` of a beat later, smoothstepped. Matt's M7 on FTR.11: *"it's the stepping itself that is the problem"*; a hard sample-and-hold holds still then jumps, and **a low turn rate is what that BUYS, not evidence against it** — the frame scored 0.30 turns/beat while the canopy read as robotic. The lesson generalises past this preset: **measure step SIZE beside step RATE, or a gate cannot tell "drifts" from "snaps"**. Slowing the clock is NOT the fix and was measured before being built: moving the hold to the BAR cut the rate ~2.5x but grew the mean step ~1.8x and the worst single event from 15 to 23 branches at once (three captures). Blend runs over raw float storage (`FeatureVector` 47 fields / `StemFeatures` 58, all `Float`, gated by `BeatHoldTests`) with `time`/`beatPhase01`/`barPhase01`/`pulseBeatIndex` restored from the live frame — a phase lerped across its 1->0 wrap runs BACKWARDS. Slot 5 is SpectralHistory on the direct-pass FRAGMENT encoder only; the mesh pipeline never binds it, the same reasoning that made slot 4 safe. Read 3 for stem data that should slide, 5 for stem data that must not change between beats. First consumer: Fractal Tree's tips (FTR.13). **FTR.10 adds a second shared binding at OBJECT/MESH buffer(4): the beat-held `FeatureVector`** (`BeatHold`, `Renderer/Geometry`) — the same struct as buffer(0), frozen at the last beat boundary, so a stateless object shader can make a layer STEP on the beat instead of sliding between beats. Generic and inherited by every mesh preset: read slot 4 for the layers that should step, slot 0 for the layers that should slide. Gated on three conditions (a bar clock has been seen, the last 8 beat intervals have sd ≤ 20 % of the mean, a beat arrived within 2× that mean) — reactive mode, beat-irregular grids (D-154) and a stalled `beatPhase01` all fall through to tracking the live vector, so the consumer degrades to continuous rather than FROZEN. Slot 4 is SceneUniforms on the ray-march fragment path only; a mesh pipeline has no ray-march pass, so the two never collide. Gated on CONSUMPTION like buffer(3) (`objectStageReadsTheBeatHeldVector`: canopy height 0.42 held against 0.92 unheld). First consumer: Fractal Tree's trunk (FTR.10). Historical note — **(D-212, FTR.4): `StemFeatures` was not bound on the OBJECT/MESH stages.** `MeshGenerator.draw()` sets only `FeatureVector` at buffer(0) and the D-057 density scalar at buffer(1). **Scope corrected FTR.2:** buffer(3) is *not* universally unset — `RenderPipeline.drawWithMeshShader` binds `StemFeatures` at **fragment** buffer(3) already (`setFragmentBytes(&stems, …, index: 3)`), so a mesh preset's FRAGMENT stage can read stems today. Only the object and mesh stages are blind, which halves FTR.4's scope to mirroring the existing `setObjectBytes`/`setMeshBytes` calls. Every mesh preset is therefore blind to per-stem and D-177 instrument-family data — no `drums_energy_dev`, no `vocals_*`, no `*_activity`. Fix is to mirror the existing `setObjectBytes`/`setMeshBytes` calls; small, but it is shared renderer code every mesh preset inherits. |
| Indirect command buffers | **DELETED — RECON.18 (2026-08-26)** | Was `RenderPipeline+ICB.swift` (340) + `Shaders/ICB.metal` (`icb_populate_kernel`) + the `.icb` RenderPass case, the `IndirectCommandBufferState` type and `ShaderLibrary`'s `supportICB` pipeline-key axis. **No sidecar ever declared `"icb"`**, `setICBState` had no production caller (the app branch logged "ICB state must be set externally" and nothing ever set it), and `supportICB: true` appeared only in tests. This row also claimed a `MeshShaders.metal` ICB populate kernel — stale; the kernel lived only in `ICB.metal`. Matt's park-or-delete call; D-203 precedent. | A GPU-driven-draw preset rebuilds it (git history: `RenderPipeline+ICB.swift`). |
| Hardware ray tracing (acceleration structures, intersector) | **DELETED — RECON.17 (2026-08-26)** | Was `Sources/Renderer/RayTracing/` (`BVHBuilder`, `RayIntersector`, `+Internal`) + `Shaders/RayTracing.metal` (`rt_nearest_hit_kernel`, `rt_shadow_kernel`). Built to capability-complete and **never wired into a shipping preset** — the row previously said so itself. Constructed only by `BVHBuilderTests` / `RayIntersectorTests`, deleted with it. Its one design-doc mention was `ARACHNE_3D_DESIGN.md` §C Option C.2, which was **not** the chosen option (C.1 screen-space was). Matt's park-or-delete call, 2026-08-26; D-203 precedent. | A future preset needing BVH traversal rebuilds it (git history: `Sources/Renderer/RayTracing/`). Metal's own `MPSRayIntersector`/`MTLAccelerationStructure` remain available — this was a hand-rolled BVH, not a platform dependency. |
| Displacement / domain warp / extrusion / revolve helpers | Supported | `Shaders/Utilities/Geometry/SDFDisplacement.metal`, `SDFModifiers.metal` | Lipschitz-safe displacement variants present. |
| Per-pass output of "anchor positions" or other geometric metadata buffers consumed by a later pass | Missing | — | Required for Arachne v8 (the WORLD pass must publish branch-attachment points so the WEB pass terminates radials on real branches). No mechanism exists. |

## 3. Material capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| PBR utility tree (Fresnel, GGX/Smith, Lambert, Oren-Nayar, Cook-Torrance, Ashikhmin-Shirley) | Supported | `Sources/Presets/Shaders/Utilities/PBR/` (V.1) | Snake_case API documented in CLAUDE.md §Module Map. |
| V.3 materials cookbook (19 surface recipes incl. chrome, brushed aluminum, gold, copper, ferrofluid, ceramic, frosted glass, wet stone, bark, leaf, silk thread, chitin, ocean, ink, marble, granite, velvet, sand glints, concrete) | Supported | `Sources/Presets/Shaders/Utilities/Materials/` (CLAUDE.md §Module Map) | Returns `MaterialResult`. Caller unpacks into `sceneMaterial` out-params for ray-march; usable directly in 2D fragment too. |
| Triplanar texturing | Supported | `Utilities/PBR/Triplanar.metal` (V.1) + procedural triplanar in `Materials/MaterialResult.metal` | Available; usage policy in Arachne is N/A (2D SDF). |
| Detail normals (UDN / whiteout combine) | Supported | `Utilities/PBR/DetailNormals.metal` | |
| Parallax occlusion mapping | Supported | `Utilities/PBR/POM.metal` (`parallax_occlusion`, `parallax_shadowed`) | |
| SSS / fiber BRDF (Marschner-lite, hair TT/TRT lobes) | Supported | `Utilities/PBR/Fiber.metal`, `SSS.metal` | Per-Arachne-v8 review, this is *secondary* for Arachne; reserved for other presets. |
| Thin-film / iridescence | Supported | `Utilities/PBR/Thin.metal` (`thinfilm_rgb`, `hue_rotate`) | |
| Image-based lighting (irradiance cubemap + prefiltered env + BRDF LUT) | Supported | `IBLManager.swift`; `Shaders/IBL.metal` | Bound at textures 9–11 for ray-march presets. |
| Refractive material with screen-space sampling of a previously-rendered texture | Missing | — | Only `refract()` math is available (Metal stdlib). No engine-supported "scene texture" to sample for refraction in a 2D preset. Required for Arachne v8 droplets (`docs/presets/ARACHNE_V8_DESIGN.md §5.8, Pass 4`). |
| Refractive material with depth-aware DoF | Missing | — | No depth source for 2D presets, see §1. |
| Per-fragment material ID routing in 2D presets | Partial | Ray-march path has materialID in the G-buffer; 2D presets typically branch on local SDF state | Works ad-hoc per preset; no engine convention. |
| Per-preset baked height texture consumed by ray-march `sceneSDF` | Supported | `UzumeEngine/Sources/Presets/FerrofluidOcean/FerrofluidParticles.swift` (1024² r16Float UMA texture + 2048-particle UMA buffer + `ferrofluid_height_bake` compute kernel in `UzumeEngine/Sources/Renderer/Shaders/FerrofluidParticles.metal`); `RayMarchPipeline.ferrofluidHeightPlaceholderTexture` (1×1 r16Float zero-filled) bound for every other ray-march preset; preamble declares `texture2d<float> ferrofluidHeight [[texture(10)]]` on every ray-march fragment + file-scope `constexpr sampler kFerrofluidHeightSampler(coord::normalized, filter::linear, address::clamp_to_zero)`. First consumer: Ferrofluid Ocean V.9 (V.9 Session 4.5b Phase 1). | sceneSDF preamble forward declaration grew one `texture2d<float>` parameter; all 5 ray-march preset sceneSDF signatures were updated. Phase 1 bakes once at preset-apply; Phase 2 (planned) replaces with a per-frame compute dispatch driven by SPH-lite particle motion + audio forces. |

## 4. Lighting capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Direct lighting via `SceneUniforms` (camera basis, key light, fog, near/far) | Supported | `PresetDescriptor+SceneUniforms.swift`; `RenderPipeline+RayMarch.swift` writes per-frame audio-reactive `SceneUniforms` | Ray-march only. Direct fragment 2D presets must synthesise their own light vectors (e.g. Arachne hardcodes `kL`). |
| Sidecar-owned forward camera dolly (`scene_dolly_speed`) | Supported | VL-PSY.4 (BUG-074): `PresetDescriptor.sceneDollySpeed` (sidecar `scene_dolly_speed`, default `0` = camera-static) → seeds `RayMarchPipeline.cameraDollySpeed`; sole reader `applyAudioModulation` (bass-modulated `× (0.5 + bass)`). | Both consumers read the descriptor: app `VisualizerEngine+Presets.applyPreset` **and** the engine-test `SessionReplayHarness` — closing the replay-harness camera-parity gap (the app-side `switch desc.name` that the engine test couldn't see is deleted). **Opt-in & byte-identical**: goldens never call `applyAudioModulation` and the integrator is 0 on frame 1. Volumetric Lithograph = 5.0. |
| Sidecar-owned camera orbit (`scene_orbit_speed`) | Supported | WHIT.2b: `PresetDescriptor.sceneOrbitSpeed` (sidecar `scene_orbit_speed`, default `0` = camera-static) → seeds `RayMarchPipeline.cameraOrbitSpeed`/`cameraOrbitAngle`; sole reader `applyAudioModulation`'s `applyCameraOrbit` (rotates camera position + forward/right/up basis around the world Y-axis through the origin). | Same dual-seed pattern as the dolly (`applyPreset` + `SessionReplayHarness`), orthogonal to it (no current preset drives both). **Opt-in & byte-identical**: angle integrator is 0 on frame 1. **No production consumer** — Rosette shipped at 0.12 (WHIT.2b), removed it (WHIT.2c, Matt live: "eliminate the orbit" — a constant unmodulated rate reads as audio-disconnected, and orbiting a camera around a wholly z=0-planar scene periodically points it near edge-on to EVERYTHING at once, flattening the whole composition rather than revealing depth), then was retired itself (D-224). Root cause both times was Rosette-specific, not the mechanism. Capability-complete and cheap to keep (mirrors RMENV.1's zero-consumer retention) for a future preset with real out-of-plane geometry and/or an audio-modulated rate. |
| Multi-light deferred lighting (up to 4 point lights) | Supported | RMENV.1: `SceneUniforms` light1/2/3 + `lightingParams.x` (count), decoded from the sidecar `scene_lights` array; `RayMarch.metal` sums Cook-Torrance over `lightingParams.x` lights. `MultiLightSceneUniformsTests`. | Key/rim/fill/accent. Only light 0 (key) casts a screen-space shadow (studio convention + flat cost). **Opt-in & byte-identical**: a preset declaring one light renders exactly as pre-RMENV, so existing ray-march goldens are unchanged. Capability-complete; **no production consumer yet** — Kinetic Sculpture was the intended first consumer (KSRB.2) but was retired before opting in (KSRETIRE.1 / D-188); RMENV awaits a future ray-march preset. |
| IBL ambient with mood-tinted lightColor | Supported | `RayMarchPipeline+Passes.swift` (per CLAUDE.md §Failed Approach #24 — ambient multiplied by `lightColor.rgb`) | Ray-march only. |
| Per-preset IBL environment selection | **DELETED — D-213 executed (RECON.14, 2026-08-25)** | Was RMENV.2: `PresetDescriptor.environment` → `IBLManager(envType:)` baking `ibl_gallery_env` in `IBL.metal`. Deleted with `IBLEnvironmentTests`; `IBLManager` bakes the single default interior again. The 2026-08-03 audit measured the consumer count as zero and structurally so (no sidecar set `"environment"`; the KSRB.2 production wiring was never built). D-203 precedent: good work is not a reason to keep code with no consumer. Recoverable from git (`git log -S ibl_gallery_env`). **RMENV.1 multi-light (`scene_lights`) is RETAINED** — three live consumers (Ferrofluid Ocean, Lumen Mosaic, Volumetric Lithograph). | A future preset wanting a reflective gallery surround rebuilds against a re-opened D-213 — do not restore piecemeal. |
| Per-preset background (environment backdrop vs sky) | **DELETED — D-213 executed (RECON.14, 2026-08-25)** | Was RMENV.3 + FLY.1: `scene_backdrop` → `lightingParams.w` dark-void / `lightingParams.y` env-backdrop branches in `RayMarch.metal`'s miss path. Both lanes are reserved again; the miss path is the original sky path (byte-identical — no sidecar ever declared either key). Same decision, reasoning, and increment as the RMENV.2 row above. | — |
| Screen-space global illumination (SSGI) | **DELETED — RECON.18 (2026-08-26)** | Was `Shaders/SSGI.metal` + `runSSGIPass`/`runSSGIBlendPass`, the `ssgi`/`ssgiBlend` pipeline states, `ssgiTexture`, the `.ssgi` RenderPass case, `PresetDescriptor.useSSGI`, the `ssgi_pass_ms` CSV column, and the D-057 `reducedMotion` OR-gate that existed solely to suppress it. **No sidecar ever declared `"ssgi"`**; both historical consumers retired (Glass Brutalist D-186, Kinetic Sculpture D-188). Matt's park-or-delete call; D-203 precedent. | A future ray-march preset wanting indirect diffuse rebuilds it (git history: `Shaders/SSGI.metal`). See the D-057 and D-167 amendments for the two decisions this touched. |
| Volumetric lighting (light shafts, god rays, dust beams) | Partial | `Utilities/Volume/LightShafts.metal` provides `ls_radial_step_uv`, `ls_shadow_march`, `ls_sun_disk`, `ls_intensity_audio` | Utility functions exist; no shipping preset composes shafts as a separate pass. Required for Arachne WORLD pass §4.2.6. |
| Volumetric fog / participating media | Supported (direct-fragment) | `Utilities/Volume/HenyeyGreenstein.metal`, `ParticipatingMedia.metal`, `Clouds.metal`, `Caustics.metal`; first consumer `Presets/Shaders/Nimbus.metal` | **Nimbus — ✅ CERTIFIED 2026-06-05 (Matt M7), the first certified `volumetric`-family preset (D-140); the first preset to compose the Volume tree** (NB.1, 2026-06-04; direct-fragment single-scatter march). **Detail cascade (NB.2, 2026-06-04):** single-coherent-body shaping via analytic envelope × *multiplicative* billow carve (distinct lumps, not a saturated solid-surface blob) + domain-warp-on-texture-coords micro filaments + multiplicative rim feathering (peeling tendrils, no hard cut); translucent σ so front-to-back accumulation reads lobe depth with no extra lighting. Recipe codified in SHADER_CRAFT §6.5 (first V.2-Volume-consumer entry). **Budget (resolved + held through NB.2):** per-step *computed* noise (`fbm4`) was over the 7 ms Tier-2 ceiling (20.2 ms @1080p); sampling the preamble `noiseVolume` 64³ 3D texture instead brought macro to **1.37 ms** and macro+meso+micro (6 taps/step) to **1.65 ms @1080p** — doubling samples cost only +0.28 ms because the body early-out keeps most steps free (NIMBUS_DESIGN §6.1/§6.2). The texture is production-bound on the direct path (`RenderPipeline+Draw.bindNoiseTextures`). No depth for 2D presets, so true depth-modulated fog is still approximate. |
| Caustics | Supported (utility) | `Utilities/Volume/Caustics.metal` | |
| Hero specular highlight | Supported | Cook-Torrance / GGX in PBR utilities; pinpoint specular trivially expressible | Used everywhere. |
| Backlit silk / fiber rim emission | Supported | `sss_backlit`, `wrap_lighting` (V.1 PBR/SSS) | Used by Gossamer, Arachne. |
| Per-pass lighting composite (e.g. apply shafts after WEB) | Missing | — | Currently lighting is per-fragment per-preset. No "compositing pass" abstraction. Arachne Pass 5. |
| Multi-light stage rig as **aurora sky parameter buffer** (slot-9 fragment buffer + matID == 2 dispatch) | Supported | V.9 Session 3 / D-125 introduced the slot-9 buffer + dispatch; V.9 Session 4.5 (D-126) reinterpreted the buffer's semantics. `Shared/StageRigState.swift` (208 B mirror) + `Common.metal` MSL struct + `RayMarchPipeline.stageRigPlaceholderBuffer` + slot-9 binding in `RayMarchPipeline+Passes.swift` (unchanged). matID == 2 branch in `Shaders/RayMarch.metal` `raymarch_lighting_fragment` rewritten to consume the buffer as **aurora-band central directions / colors / intensities** rather than as point-light positions for Cook-Torrance. `FerrofluidStageRig` (first consumer; doc comment updated to reflect the new semantics). Test gate: `StageRigStateLayoutTests.test_stageRigState_strideIs208` + `testFerrofluidOceanSkyReflectionDispatchActive` (renamed from `testFerrofluidOceanStageRigDispatchActive`). | The §5.8 musical contract (vocals_pitch → palette, drums_energy_dev → intensity, arousal → orbit speed) is preserved at the rig boundary; only the GPU consumption changed. Failed Approach #61 records why the original Cook-Torrance per-light interpretation was retired (inverse-square attenuation at orbit altitudes 6 m / radius 4 m gave invisible beams against the IBL the mirror also reflected). Reusable for any future "near-mirror substrate reflecting audio-driven sky" preset. |
| Mirror-reflects-procedural-sky lighting paradigm (matID == 2; `rm_ferrofluidBaseSky` + `rm_ferrofluidSky` in `Shaders/RayMarch.metal`) | Supported | V.9 Session 4.5 Phase A / D-126: `rm_ferrofluidBaseSky(R, scene)` produces a three-stop dark-purple gradient (below-horizon / horizon / zenith stops) × `scene.lightColor.rgb` for D-022 mood propagation; `rm_ferrofluidSky(R, rig, scene)` adds anisotropic aurora-stripe bands per active rig light using `vertFalloff × azim_falloff × bandIntensity`. matID == 2 branch in `raymarch_lighting_fragment` computes `R = reflect(-V, N)`, samples sky at R, multiplies by thin-film F0_thin at NdotV, mixes toward base-sky-at-zenith for atmospheric depth — `rm_finishLightingPass` is **bypassed** entirely (no diffuse IBL, no separate fog tail; the sky function is the IBL for this material). Test gates: `testFerrofluidOceanMoodTintAtmosphereShifts` (fog enabled), `testFerrofluidOceanMoodTintSkyBaseShift` (fog disabled, exercises sky-base lightColor multiply alone). | Replaces Session 3's Cook-Torrance per-light path that matID == 2 originally shipped (Failed Approach #61). Reusable for any near-mirror metallic substrate that should reflect an audio-driven sky rather than respond to discrete light sources. Aurora bands sit at a single elevation (orbit altitude / radius determines bandDir.y ≈ 0.83); multi-elevation curtains per `08_lighting_aurora_over_dark_water.jpg` would require per-light orbit altitudes (out of scope for V.9 Session 4.5). |
| Audio-modulated thin-film thickness (matID == 2 / matID == 3 metallic substrate, baseline ± arousal × range) | Supported | V.9 Session 4 Phase B: matID == 2 and matID == 3 branches in `Shaders/RayMarch.metal` compute `kFerrofluidFilmThicknessNm = baseline + clamp(features.arousal, -1, 1) × range`; the MSL constants (220 nm baseline / 40 nm range) are the single source of truth — the `ferrofluid` sidecar block that once mirrored them was decoded but never read, and was deleted at RECON.22 | Range [180, 260] nm stays inside the subtle blue-to-cyan band (SHADER_CRAFT §10.3.x); the rainbow oil-slick failure mode lives above ~300 nm. Under Session 4.5 the thin-film F0 multiplies the sky reflection directly (matID == 2 bypasses `rm_finishLightingPass`); matID == 3 single-light path is unchanged and still composes through the shared lighting tail. |
| Smooth Voronoi utility (Inigo Quilez exp/log soft-min over 9 neighbor cells) | Supported | V.9 Session 4.5 Phase A: `Presets/Shaders/Utilities/Texture/Voronoi.metal` `voronoi_smooth(p, scale, k)` returns `-(1/k) × log2(Σ exp2(-k × dᵢ))` over the 9 neighbor cells. Same hash + cell-offset infrastructure as `voronoi_f1f2`; returns scaled-space distance (divide by `scale` for world space). | The C¹-continuous alternative to regular Voronoi's hard-min — eliminates cell-boundary normal-flip artifacts in height fields built from Voronoi distance. Reference: https://iquilezles.org/articles/smoothvoronoi/. First consumer: Ferrofluid Ocean spike field. Reusable for any height-field / SDF that needs continuous gradients across what would otherwise be cell-edge creases. |

## 5. Atmosphere capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Procedural sky / horizon gradient | Partial | Trivially expressible in any 2D fragment (`mix(botCol, topCol, uv.y)`). No shared utility. | If multiple presets want it, lift into a Color or Atmosphere utility. |
| Atmospheric mist / fbm-based haze | Supported | `Utilities/Noise/FBM.metal` (`fbm4/8/12`); used in Volumetric Lithograph and Arachne | |
| Aerial perspective (depth-modulated colour shift) | Partial | Available in ray-march via `apply_fog`; not generalised to 2D | |
| Dust mote field (hash-lattice or fbm-thresholded) | Partial | Authored ad-hoc per preset (Arachne, planned for Gossamer). No shared utility. | Worth promoting to `Utilities/Atmosphere/`. |
| Light shafts as a composable pass | Missing | Utilities exist (`Utilities/Volume/LightShafts.metal`) but no preset uses them as a composited pass | Arachne WORLD §4.2.6. |
| Aurora / large-scale sky structure | Missing | — | Arachne high-arousal variant (ref 15). |
| Forest-stage layered backdrop (sky + distant trees + mid trunks + near branches + floor + atmosphere) as a reusable preset module | Missing | — | First instance will be the Arachne WORLD pass. Should be authored so future "in-the-world" presets (Gossamer revisit, future Stalker variants) can reuse. |

## 6. Motion / state capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Per-vertex feedback warp (mv_warp) with decay | Supported | `RenderPipeline+MVWarp.swift`; `Shaders/MVWarp.metal` | Required for Milkdrop-class temporal accumulation (D-027). |
| mv_warp custom-warp transfer + NO-decay self-regulating feedback | Supported | `PresetLoader+WarpPreamble.swift` `mvWarp_fragment` (normalise + hue-zoom resample + R→G→B transfer, gated by `chromaticMix`; `decayMul=1` on the custom-warp path) | Faithful butterchurn custom-warp loop (D-138, Dragon Bloom). The transfer self-regulates the feedback (no separate decay); other presets gated out (byte-identical). |
| mv_warp "marks on top" feedback (scene-geometry composited normal-alpha onto the warped frame) — **per-preset** | Supported | `RenderPipeline+MVWarp.swift` `drawWithMVWarp` (marks-on-top branch when `sceneGeometryState != nil`); `RenderPipeline+SceneGeometry.swift`; geometry pipeline resolved per-prefix in `PresetLoader.makeSceneGeometryPipeline` (`<prefix>_geometry_*`, legacy `dragon_bloom_strand_*` fallback) | Replicates butterchurn's draw-waves-on-target loop (D-138). **Generalised per-preset in Skein.ENGINE.1.1 (D-143):** any mv_warp preset draws its own overlay; draw params + chromatic + comp + beat pump come from the preset's `marks` descriptor block. Instances: Dragon Bloom (strands, 1536/3/lineStrip, chromatic 1.0, comp+beat), Skein (a fullscreen-triangle swept-capsule **pour line** at Skein.1 + **hash-seeded splatter morphology** — ragged droplet bursts + filaments + a viscosity axis — at Skein.2; was the ENGINE.1.1 test disc — chromatic 0, comp identity, no beat). The overlay **vertex** reads `features@0` (the `dragon_bloom_strand_vertex` slot), so a preset can drive a time-varying overlay (Skein's painter + debug viscosity = `f(features.time)`) and generate per-fragment marks from the Noise/Hash utilities with **no engine touch / no per-preset buffer** (Skein.1 + Skein.2 path A). Byte-identical for both (DB/FM accumulation tests + PresetRegression). |
| Canvas-hold: lossless feedback accumulation (identity warp + no-decay + no-transfer + marks-on-top) | Supported — **config of the existing mv_warp machinery; no engine addition** (Skein.ENGINE.1 audit, D-142) | Identity: preset `mvWarpPerVertex` returns `uv` + `mvWarpPerFrame` zoom=1/rot=0/offsets=0 (`Skein.metal`). No-decay: `mvWarpPerFrame` decay=1.0 → `decayMul = (chromaticMix>0)?1.0:in.decay` (`PresetLoader+WarpPreamble.swift:206`). No-transfer: `mvWarpChromatic=0` default (`RenderPipeline.swift:50`). Marks-on-top: the row above. Proof: `SkeinCanvasHoldTest` (whole-frame **Hamming 0 across 130 hold frames**, live scene→warp→blit→swap). | The no-decay / identity configuration of the brush-on-feedback paradigm (D-135 / D-138) — a sibling of Dragon Bloom, **not** a new render mode and **not** a D-029 concern (D-142). The four properties are independent (no-decay is NOT bound to the colour transfer). Reachable as pure per-preset config — no UzumeEngine source change — so every other mv_warp preset is byte-identical by construction. sRGB 8-bit feedback is lossless here (round-trip + identity-at-pixel-centers both exact; no linear-format / nearest-sampler override needed). First consumer: Skein.ENGINE.1; **Skein renders live at Skein.ENGINE.1.1** (D-143) — cream ground from the per-preset canvas clear + the test disc through the now-per-preset marks-on-top overlay; the **wandering pour line replaces the disc at Skein.1** (`57ee7383` — path A, the overlay vertex reads `features@0`, no engine touch). |
| Canvas-hold **wetness channel** (transient per-pixel "freshness" in the feedback ALPHA; stamp-where-painted + decay-pauses-at-silence; read at the display stage) | Supported (Skein.ENGINE.2, D-149) | Storage: the feedback texture's ALPHA (linear 8-bit on `.bgra8Unorm_srgb`; RGB = the lossless paint record). Stamp: the existing marks-on-top overlay alpha-over blend (`A = bestCover²+dst.a(1−cover)`, `RenderPipeline+SceneGeometry.swift`). Decay: `skein_warp_fragment` (`<prefix>_warp_fragment` override) does `A *= wetnessDecay`, RGB held byte-identical; `mvWarpWetnessDecay` uniform at warp-fragment `buffer(1)` (`RenderPipeline.swift` `mvWarpWetnessDecay` / `+PresetSwitching.swift setMVWarpWetnessDecay` / `+MVWarp.swift` bind), default 1.0; `wetnessDecay = exp(-rate·dt·stemMix)` from `SkeinState` pauses at silence. Read: `skein_comp_fragment` reads `.a`. Proof: `SkeinWetnessTest` (stamp 255 / monotone decay 253→172 under music / silence spread 0, live path). | A **gated additive** signal — `default 1.0` ⇒ A held; only `skein_warp_fragment` reads `buffer(1)`, and FM never runs the standard warp pass, so DB/FM/Starburst are **byte-identical by construction** (no shared GPU code touched — the per-prefix override). Drying is a **read-time** effect on a **separate** channel (`SKEIN_DESIGN §5.5`), never a destructive RGB multiply — the RGB lossless-hold (Hamming-0, now RGB-scoped) is intact. **Not a new audio primitive** (reuses the silence gate, FA #67). **Cut-line check (D-149): PASS — no shared format change, no new pass, no loop reshape.** First/only consumer: Skein.4's wet/dry sheen. |
| mv_warp per-preset canvas-clear ground colour | Supported (Skein.ENGINE.1.1, D-143) | `MVWarpPipelineBundle`/`MVWarpState` `canvasClearColor`; `RenderPipeline+MVWarp.swift` `clearWarpTextures(to:)`; sourced from `marks.canvas_clear` in `VisualizerEngine+Presets.swift` | The mv_warp feedback textures clear to a per-preset colour (was hard-coded black). On the marks-on-top path Pass 0 is skipped, so this clear IS the held ground (Skein's cream). Black default ⇒ every existing preset byte-identical; carried on `MVWarpState` so drawable-resize re-clears to the same ground. |
| mv_warp comp-stage display effects (video echo / gamma / invert / beat pump) | Supported | `mvWarp_blit_fragment` (float4 `post` uniform via `setMVWarpPost`; `(0,0,1,0)` ⇒ identity) | Milkdrop fixed-function comp (echo orient-1 mirror, `×gamma`, invert) + a beat-pulse pump/brighten — display-only, not fed back (D-138). The beat pump is **per-preset** since D-143 (`setMVWarpPost beatPulse:`, from `marks.beat_pulse`; was keyed on `sceneGeometryState != nil`) — Dragon Bloom on, Skein off. 8-bit feedback format is load-bearing (the per-frame clamp holds the saturated equilibrium). |
| Preset-overridable custom warp / comp / blur fragments (per-preset render loop) | Supported (2 consumers) | `PresetLoader.swift` prefix lookup (`<prefix>_warp/comp/blur_fragment`, falls back to shared `mvWarp_*`); `FataMorgana.metal` + `RenderPipeline+FataMorgana.swift`; `Nacre.metal` + `RenderPipeline+Nacre.swift` | A preset can supply its OWN warp+comp(+blur) fragments and a dedicated draw branch. **Fata Morgana** (D-139): custom feedback warp + procedural comp projection + a 1/4-res `blur1` target (branch keyed on `blurState != nil`). **Nacre** (D-171, NACRE.2b): custom feedback warp (inline wide-gaussian unsharp — NO separate blur target) + fully-replacing signature comp; branch keyed on the `isNacre` bundle/state flag (checked before the blur heuristic). Per-frame uniforms (`FataUniforms` / `NacreUniforms`) are CPU-computed and bound at fragment buffer(1) of the custom passes. Presets without the symbols are byte-identical (PresetRegression). Generalises the D-138 custom-warp path to a fully preset-owned loop. |
| Custom procedural comp projection (perspective mirage + reflection + starfield) | Supported | `FataMorgana.metal` `fata_morgana_comp_fragment` (perspective floor `z=0.2/|uv.y|`, horizon glow, water reflection, point-wrap grid stars) | Display-only procedural projection of the feedback field into a scene (D-139). Custom comp fully replaces fixed-function gamma/darken/echo (butterchurn convention). |
| sRGB round-trip for sRGB-naive ported shaders | Supported | `FataMorgana.metal` comp output sRGB-decode before the `.bgra8Unorm_srgb` drawable write | Cancels Metal's sRGB encode so an sRGB-naive source shader's output maps to its intended display values (D-139, FA #71). Feedback textures stay linear `.bgra8Unorm` (un-decoded). |
| Bar-synced coordinated shape motion (phase-offset sway over the feedback field) | Supported | `RenderPipeline+FataMorgana.swift` `fataSwayClock` (+1/bar from `barPhase01`); `FataMorgana.metal` `fata_shape_vertex` per-shape phase offsets | A few shapes share a `cos(π·swayClock)` sway, phase-offset to stay frame-balanced and turn on each downbeat (D-139). Bar grid drives it; frozen when absent. |
| CPU-side polyline trail as an exact record (ring buffer → instanced sprites + line strip, NOT a feedback accumulator) | Supported (WL.2, Witchlight) | `Renderer/Geometry/WitchlightPath.swift` + `WitchlightStroke.swift`; `Renderer/Shaders/Witchlight.metal` (`witchlight_suppress_*` / `_line_*` / `_bead_*` / `_flare_*`) | A `feedback+particles` preset whose trail lives in a ~1024-entry CPU ring buffer rather than in an accumulator, so the visible stroke is an EXACT record of where the pen has been rather than a decayed image of it — required by Witchlight's concept (the figure IS the last 30 s of harmonic motion) and by the beads-on-a-thread register, which a warp would smear. No compute pass: 1024 beads are cheaper to advance in Swift than to dispatch, and the age-weighted Laplacian relaxation reads each bead's two neighbours. Four draws off one buffer, including a **dst \*= (1−a) darkening pass** (star suppression behind the ribbon) — the first non-additive, non-alpha-over blend in a particles pass. Sprites are instanced quads sized in SCREEN space, not `[[point_size]]`: pixel-sized sprites occupy a different frame fraction at every drawable size, which breaks any frame-AREA safety budget. **WL.2-g — glow reach decoupled from element size.** The bead quad is expanded by `WL_HALO_EXTENT` (2.6×) and the core's radial term rescaled by the same factor, so the halo can be *wider* than the bead (the two-part profile every reference cross-section of a glowing line shows) while the core stays pinned to the settled bead size. This is the reusable part: for sprites emitted along a path, overlapping halos are what merge a row of dots into a continuous thread — raising emissive level cannot, because the gaps between sprites contain no geometry to light (measured: near-doubling level moved above-threshold pixel share 0.405 % → 0.421 %; the quad expansion moved it → 0.777 %). Caveat for any reuse: the pass blends ADDITIVELY, so once halos overlap the per-sprite level that reads correctly in isolation sums into a uniform tube — set the core/halo split against the overlapped result. `SHADER_CRAFT.md §13` #51–#53. |
| Bounded-curvature (Dubins) path generation from a continuous audio driver | Supported (WL.2, Witchlight) | `Renderer/Geometry/WitchlightPath.swift` `advancePen`; `WitchlightPathTests` | Fixed governed speed + turn rate clamped to ±v/R_min, so every arc has radius ≥ R_min regardless of how fast the driver moves. Built to absorb a measured ~10× cross-track spread in the harmonic rate (WITCHLIGHT_DESIGN §2.3) as a MECHANISM rather than a tuning constant. **Measured limitation, WL.2:** because `θ̇ = k·φ̄̇` integrates to `θ ≈ k·φ̄`, a driver that reverses at high frequency around a concentrated mean produces a near-straight path, and the gain `k` cannot fix it — saturating the clamp makes the path straighter, not more curved (verified at k = 1.1 / 2.6 / 5.0). Anyone reaching for this capability to draw a *figure* from a circular audio phase should read that result first. |
| Grid-locked accent with a reactive fallback and self-gating cold start | Supported (WL.8, Witchlight) | `Renderer/Geometry/WitchlightPath+Events.swift` `advanceFlare`; `WitchlightPath.barDownbeatNow` / `gridSilentFor`; probe `WitchlightBeatAlignmentProbe` | **`barPhase01` IS the grid-trust signal** — it is documented as *always 0 in reactive mode (no BeatGrid installed)*, so a route keyed on its WRAP is automatically silent on tracks with no usable grid, and D-154's "beat-irregular tracks excluded" costs no extra field, no confidence threshold, no config. Track how long it has read zero (~2 s) to distinguish "no grid" from "between wraps" and fall back to a continuous-energy trigger, so those tracks keep an accent instead of losing the feature. Cold start is the preset's own job per the phase contract: the cached grid installs with reliable BPM/meter but possibly WRONG PHASE, so ramp the accent in over `trackElapsedS` (2 → 8 s) — and scope that ramp to the grid route only, because a fallback keyed on an energy excursion has no phase to be wrong about. **The measurement that justifies any of this**: score accent times against grid-derived beat times (bar wraps + `60/grid_bpm` steps) and report mean |offset| in beats — 0.25 is the value uniformly-random firing produces, so it is the null hypothesis, not a good score. Witchlight's pre-WL.8 accent measured 0.247. **Do NOT score against `beatPhase01`**: on a real 171 BPM session it wrapped 24 times where 614 were due, and a stalled reference makes every candidate look like chance. **And the trap that hides all of it**: the cold-start ramp reads `trackElapsedS`, so any harness that leaves it at 0 suppresses the accent entirely while the route looks correct in source — including the flash-safety harness, which then certifies a worst case that contains no accent. |
| Auto-framing camera for a continuously-advancing generator (scale fit and camera aim on SEPARATE points) | Supported (WL.7, Witchlight) | `Renderer/Geometry/WitchlightPath+Events.swift` `reframe`; `WitchlightPath.cameraX/cameraY`; gated by `WitchlightHeadFramingTests` | Any preset whose subject ADVANCES (a pen, a head, a leading edge) and whose camera auto-fits to the whole trail has a defect built in: the leading edge sits at the extreme of the extent, while an RMS fit covers ~0.6 of it, so **the newest part of the drawing is off screen almost by construction**. Measured on Witchlight before the fix: the burning head — where the flare, the brightest beads, the per-frame energy breath and the silence gate ALL show — was off frame on **33 / 35 / 45 %** of frames across three fixtures. Every audio route was present, correct and QG.5-gated, and roughly two frames in five the viewer could not see any of them; six M7 rounds read it as "not tied to the music". **The fix is to stop using one point for two jobs**: fit the scale about the trail's centroid (unchanged), and aim the camera at a head-biased point (60 % toward the head, 1.2 s follow) — `viewScale` is then bit-identical and element legibility cannot regress. The obvious alternative — fit MORE into frame — shrinks the drawing and fails a distinctness gate; it was tried three times at WL.6 (23 → 4 distinct beads). What it costs is the far tail leaving frame, which for an ageing trail is the dimmest, oldest content. **Reusable rule: measure where your subject IS in NDC before adding another audio route.** A CPU mirror of the vertex projection is enough — no GPU, no PNGs. |
| **Frequency-to-visible-wavelength dispersion** of the live waveform (band-split → per-band spectral colour → amplitude-driven spatial fan) as a `ParticleGeometry` | Supported (CHR.3b, Stave) | `Renderer/Geometry/StaveDispersionModel.swift` + `StaveTrace.swift`; `Renderer/Shaders/StaveTrace.metal` | The first preset in the catalog whose COLOUR is a physical quantity rather than a palette: audible frequency mapped once onto 700–400 nm and converted with Bruton's spectral fit, so a red curve IS low frequency by construction. Reusable lessons: **(a)** the raw waveform is reachable CPU-side by handing the geometry the engine's `waveformBuffer` (the Meniscus MEN.2b precedent) — no engine surface, no protocol change, and it is the ONLY way to get pre-AGC, pre-smoothing signal into a preset; **(b)** band-splitting needs no FFT — a moving-average cascade (box of width W = low-pass at fs/2W, consecutive differences = band-passes) costs ~8 prefix-sum passes over 1024 samples; **(c)** do NOT space bands one per octave for a colour mapping — high frequencies oscillate densely and clump the violet end into a single mass; weight the plan toward the low end where the energy is; **(d)** compensate the spectral tilt only PARTIALLY (exponent < 1) — full normalisation equalises the top bands into a spiky comb and produces the cheap-rainbow read; **(e)** a FIXED spatial separation fails on smooth quiet material, collapsing into static parallel stripes once the wave excursions go small next to the gaps — drive the separation from on-screen amplitude, which additionally supplies quiet-vs-loud contrast on limited masters (measured: 1.4× RMS across a whole modern song, so level alone carries nothing); **(f)** per-band level needs a LONG (~20 s) envelope, not a per-section normaliser and not a fixed gain — long enough that a quiet passage still draws quieter, short enough to settle a track change without knowing the track in advance. ⚠ Not gateable by QG.1: the waveform buffer is not a session-recordable primitive, so a waveform-driven preset has an empty `audio_routes` and cannot certify until a routable waveform-derived primitive exists. First consumer: **Stave** (`certified: false`). |
| Structural-section signal reaching a `ParticleGeometry` | Supported (WL.2) | `VisualizerEngine+Presets.swift` `bindWitchlightRuntime`; `WitchlightPath.ingestStructure` | `StructuralPrediction` is CPU-only and is not in the `ParticleGeometry.update(features:stemFeatures:commandBuffer:)` signature, so a particles preset cannot see `sectionIndex` at all. Witchlight reaches it by ALSO joining `StatefulRuntimeRegistry` and binding a tick that carries nothing but the structure — no slot-6 buffer. The Skein.ENGINE.3 / D-151 bridge, reused. Ordering: the tick fires after `particles?.update(...)`, so the index lands one frame late — not observable on a per-section event. |
| Feedback ping-pong texture | Supported | `RenderPipeline+FeedbackDraw.swift` | Pre-mv_warp generation. **CLEAN.4.4**: the ~32 MB @ 4K pair is allocated only for presets that **sample** it — surface-mode feedback (Membrane: warp → composite → blit). Particle-mode feedback presets (Murmuration: draw straight to the drawable) and all non-feedback presets allocate **none**, gated by `RenderPipeline.activePresetSamplesFeedback` at the resize handler + the `renderFrame` lazy-alloc; the pair is freed on switch-away (`setFeedbackParams(nil)`). The particle-mode warp pass — which wrote a texture nothing read — is skipped. Output-preserving (PresetRegression goldens byte-identical). |
| Compute-driven agent-network simulation (atomic deposit accumulator + ping-pong trail map) as a `ParticleGeometry` | Supported (PHYS.1, Filigree) | `PhysarumGeometry.swift`; `Physarum.metal` (`physarum_reset` / `_agents` / `_diffuse`) | Physarum / slime-mold class: N agents sense a trail (toroidal `address::repeat`), steer (Jones), deposit into an `atomic_uint` accumulator; a diffuse+decay kernel ping-pongs an `r16Float` trail; drawn fullscreen in particle mode. One compute encoder, `memoryBarrier(.buffers)` between dependent dispatches (Metal does not serialise consecutive dispatches; cf. `FerrofluidParticles.swift`). Proven ~0.66 ms/frame @ 1080p, 262k agents (1M ~0.55 ms). **No new render primitive** — existing per-frame-compute + ping-pong + particle-mode draw. |
| Compute-driven reaction–diffusion field (multi-channel ping-pong, N substeps/frame) as a `ParticleGeometry` | Supported (MITOSIS.0/.1, Mitosis) | `MitosisGeometry.swift`; `Renderer/Shaders/Mitosis.metal` (`mitosis_react` / `mitosis_fragment`) | Gray–Scott reaction–diffusion: one `rg16Float` (A,B) ping-pong texture pair, N react substeps/frame in one compute encoder with `memoryBarrier(.textures)` between dependent substeps; B-channel colorized fullscreen in particle mode. No agents, no accumulator (simpler than the agent-network row). Onset paints B nuclei into open space (cells bud on the beat); energy shifts kill-rate across the death boundary (merge↔divide). Proven ~0.65 ms/frame @ 1080p (320×180 sim, ≤18 substeps; ~25× headroom). **No new render primitive.** Delivers the tight onset→event sync the agent-network trail substrate could not (`FILIGREE_DESIGN §"sync finding"`). |
| Explicit CPU cell-list preset rendered by a fullscreen composite fragment (no compute, no textures) as a `ParticleGeometry` | Supported (MITOSIS-G2.1, Cytokinesis) | `MitosisGen2Geometry.swift`; `Renderer/Shaders/MitosisGen2.metal` (`mitosisgen2_fragment`) | The detailed/"realistic" cell-division path: a small CPU `Cell` array (cap 8) advanced on the CPU in `update` (no compute encoder — phase/snap/cull governor), packed via `setFragmentBytes` (≤ 4 KB, no MTLBuffer) + aspect from `features.aspectRatio` to a fullscreen fragment that procedurally composites each cell as an OPAQUE object (smooth-min dumbbell + asters + occluding membrane + cortex + chromatin + midzone) over a filament field. Resolution-independent (no sim grid → no blur lever). ~4.2 ms/frame @ 1080p, 8 cells. **No new render primitive.** Distinct from the RD row above: no field, no ping-pong; the substrate is explicit objects, so it can draw per-cell internal detail RD cannot. First consumer: **Cytokinesis** (Mitosis gen-2). |
| Onset-driven gestural MARKS with per-voice colour and articulation-selected form (CPU pen model → per-gesture deposit into an HDR trail → display over a painterly colour-shifting ground) as a `ParticleGeometry` | Supported (RICERCAR-ECHO; **CERTIFIED, RICERCAR-CERT.1, 2026-08-20**) | `RicercarEchoGeometry.swift`; `Renderer/Shaders/RicercarEcho.metal`; backdrop `Presets/Shaders/Ricercar.metal` | Fantasia *Toccata & Fugue* as the north star, taken as spirit and not recast. A fugue is repetition with variation, so a note attack births a mark that echoes and imitates itself as the music repeats. **★★ The load-bearing lesson is the sync, and it was expensive:** marks fire ONLY on real note ONSETS — nothing on a timer, rate, or sustain-fill, because any fill "puts marks where there are no notes" and the pure-onset version's opening was the first thing Matt called *"perfect"*. The detector is a RELATIVE local transient `(levFast − levMed)/levMed` on the AGC band LEVELS with a fast (τ≈0.04) baseline. It must be relative: an absolute rise, a change-above-slow-average, and the deviation primitives all FLATTEN under AGC during sustained music, which is what produced *"great for 5 s then goes to shit"*. **Do not tune the onset block.** Articulation (a slow level floor against the fast baseline) picks the FORM — legato a long flowing line, staccato a short clip, pizz a dot. **Audio:** emission ← `bass`/`mid`/`treble` levels (onset); sharpness ← `bass/mid/trebDev`; **colour ← instrument-family activity** (`StemFeatures.*ActivityDev`, D-177/IFC.4) — the app's FIRST consumer of PANNs family capture. ⚠ Family activity is supplied by the pipeline but its weights are gitignored; a worktree without them silently renders fallback colour with no error (`family: 0 windows`), which is why `RicercarEchoWiringTests` asserts the colour reaches the PICTURE rather than the inputs. Gated by `RicercarEchoWiringTests`; the look was judged from rendered contact sheets against Matt's eye. First + only consumer: **Ricercar**; `certified: true`. Strokes drawn as a continuous ribbon (capsule quad per pen-to-pen segment, distance-to-segment shading), not a point-sprite deposit — that mechanism read as simultaneously blurry and beaded at any resolution or falloff tuning (RICERCAR-WIRE.3, Matt: *"Why does it have to be a dotted line?"*). |


## 7. Audio-reactivity capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Continuous energy bands (3-band + 6-band, instant + attenuated) on GPU | Supported | `BandEnergyProcessor` → `FeatureVector` floats 1–24 | Primary visual driver per CLAUDE.md §Audio Data Hierarchy. |
| AGC-normalised deviation primitives (`xRel`, `xDev`, `xAttRel`) | Supported | `FeatureVector` floats 26–34 (`BandDeviationTracker`); `StemFeatures` floats 17–24 (D-026, MV-1; D-146/AGC2) | Required style. Absolute-threshold patterns are an anti-pattern. **The band-`*Dev` pivot is each band's own running average since D-146/AGC2 (2026-06-06)** — `midDev`/`trebDev` now fire on real music (were structurally ~0 % under the old fixed-0.5 pivot, BUG-027). Mid/treble carry smaller amplitude (use a larger gain) + a ~1-2 s cold-start warmup; SHADER_CRAFT §14.1. Stem `*EnergyDev` already used a per-stem EMA (unchanged). **⚠ UPDATE RATE IS PATH-DEPENDENT, AND THE LOCAL-FILE FIGURE IS NOW FLAG-DEPENDENT (BUG-087).** Streaming: **58.8 Hz** measured, essentially render rate. Local file, tap-driven (today's default): **10.01 Hz** measured on `2026-09-10T22-07-34Z` — capped by how often audio ARRIVES (every 100 ms), not by processing, which is why BUG087.3's slicing raised computation to ~47 Hz and moved what a preset observes to only ~16. Local file with **`UZUME_LF_ANALYSIS_CLOCK=1`** (BUG087.4, 2026-09-10): **59.2 Hz observed** — the analysis clock reads the decoded file at the smoothed playhead at 80 Hz instead of waiting for the tap, so the arrival ceiling is gone. **Until that flag is default-on, assume 100 ms steps on local files** and do not expect the streaming rate there; after it is, the two paths agree. ⚠ Either way, the rate is NOT the same thing as the lag: ~135–170 ms of every continuous primitive is deliberate `BandEnergyProcessor` smoothing (τ 77 ms bass / 116 ms mid-treble) that BUG087.4 does not touch. Original diagnosis: — the analysis runs once per audio callback with no time gate, and AVAudioEngine hands `LocalFilePlaybackProvider` 0.1 s buffers regardless of its `bufferSize: 1024` request (measured 100.0–100.2 ms on 8 of 10 captures, at both 44.1 and 48 kHz). Local files are the path essentially all preset work runs on, so **assume 10 Hz, i.e. 100 ms steps, for anything that reads a primitive per frame** — a preset drawing directly from these at 60 fps is sampling a step function, not a curve. Measure any capture with `Scripts/measure_analysis_rate.py`. |
| Beat onset pulses (per-band + composite) | Supported | `BeatDetector` → `OnsetPulses` → `FeatureVector` | Accent-only by policy. |
| Beat-phase clock (analytic, drift-tracked) | Supported (post-DSP.2) | `LiveBeatDriftTracker` + offline `BeatGrid` (DSP.2 S5–S8) | For anticipation curves and beat-anchored motion. **Caveat (FBS Stage 0):** the live drift correction wanders 50–90 ms over a track's opening — motion synced to it can read as "searching." For dead-steady per-beat accents prefer the steady pulse row below. |
| **Steady first-note-anchored SLOW pulse (`pulse_phase01`/`pulse_amp01`, 4 beats/cycle, never drift-corrected)** | Supported (FBS, D-153 + D-154 + D-156) | `BeatPulseClock` (`Sources/DSP/`) → `FeatureVector` floats 40–41 (reclaimed `_pad4`/`_pad5`); tempo from the cached `BeatGrid` via `MIRPipeline.setBeatGrid`; anchor resets per track in `MIRPipeline.reset()`; logged as trailing `features.csv` columns | Anchored at the track's first NOTE (silence→sound, 3-frame confirm, backdated), one cycle per FOUR beats at the cached-grid tempo (~1 % err, Stage 0; D-154 slow pulse — a per-beat punch from an arbitrary playlist anchor read as a robotic metronome), never corrected during the opening; after ~10 s it HANDS OFF invisibly (rest-window swap, D-156) to `LiveBeatDriftTracker`'s per-beat phase for the energetic steady state. Presets consuming it should pair with `requires_regular_beat` in their sidecar so beat-irregular tracks are excluded at the picker (D-154). `pulse_amp01` gates silence (0 before the first note / across > 0.5 s sustained silence) — **measured WL.1: it sits at 1.000 on 98.7 % of live-session frames, which is the gate working correctly. It is a GATE, not an envelope: it has no dynamic range and must never be cited as a driver. `pulse_phase01` is the driver** (full 0–1 sawtooth, measured alive). Phase is perceptually-convincing, not provably the downbeat (FA #69 limit); gapless mid-playlist segues anchor at the track-change instant. First consumer: Ferrofluid Ocean spike punch (`fo_spike_strength`). Available to any preset needing a steady per-beat accent that must not wander. |
| Per-stem energy + per-stem deviation primitives | Supported | `StemFeatures` floats 1–24 | **Every per-stem field carries ≈2.5 s of latency** (was ≈5.4 s, measured; BUG-086 / BUG086.1, 2026-08-11). The floor is the separation period: `latency = stemChunkSeconds − stemReadStartSeconds`, which must cover one period or the read window clamps and features freeze. Chunk length is pinned by the exported Open-Unmix model (`modelFrameCount = 431`), so the period is the only lever, at one full inference each (~7 % duty at 2 s). **Authoring consequence: per-stem fields are section-and-envelope drivers, not transient drivers, and must never be paired against the time-aligned beat grid** (`grid_bpm` / `beatPhase01`, ≈0.3 s) in a way that asks the viewer to see them agree — that mismatch is what BUG-086 made visible. For sub-second coupling use the band primitives. Constants + invariant: `VisualizerEngine+Stems.swift` §"Separation cadence and read alignment"; gate: `StemSeparationCadenceRegressionTests`. |
| Per-stem rich metadata (onsetRate, centroid, attackRatio, energySlope) | Supported **as activity level; NOT as an instrument identity (FTR.12, 2026-08-12)** | `StemAnalyzer` → `StemFeatures` floats 25–40 (MV-3a); measurement `docs/diagnostics/FTR12_GUITAR_CHANNEL_2026-08-12.md`, harness `GuitarChannelReportTests` + `Scripts/ftr12_guitar_channel.sh` | Same ≈2.5 s latency as the row above — these are computed from the same delayed read window. `{stem}OnsetRate` in particular is **not** a live onset signal; it describes onsets from ≈2.5 s ago. **⚠ AND IT DOES NOT IDENTIFY WHAT IS PLAYING.** Measured across 7 tracks (2 solo/clean-guitar positives, 3 guitarless negatives, 2 distorted-rock hard cases): `r(otherOnsetRate, drumsOnsetRate)` is **highest at +0.792 on a solo piano recording with no guitar and no drum kit** and lowest at +0.492 on Seven Nation Army; p50 spans just **4.06…5.33** across solo classical guitar, player piano, pure synthesis and distorted rock. On the solo-classical-guitar record the drums stem — pure separation residue — yields a **higher** onset rate than the guitar (4.71 vs 4.46), and `otherOnsetRate` correlates **+0.895 with `vocalsOnsetRate`** on a record with no voice. **Mechanism:** `computeRichFeatures` fires on broadband RMS flux past an **adaptive** threshold (`fluxEMA × 1.5`, 100 ms refractory → 10 /s ceiling; corpus sits at 4–5 /s), so it measures the detector, not the instrument. **Authoring consequence: never label a route on one of these fields with an instrument name, and never justify one from a single track's correlation** — the two capture figures behind Fractal Tree's tips route disagreed by 0.57 (+0.14 Cherub, +0.71 SNA) on the same feature. `energyRel`/`energyDev`/`energySlope` are worse still: r 0.81–0.99 vs drums on **every** track, common mode 91–99 % against CHR.1's ≈22 % null. Extends MEL.1 / FA #68 with a different failure mode (content-independent rate, not smeared attacks). |
| Instrument-family capture (IFC.3/IFC.4) — `stringsActivity[Dev]`, `brass…`, `woodwinds…`, `percussion…` | Supported | `InstrumentFamilyAnalyzer` + `InstrumentFamilyActivity` (`Sources/ML`) → `StemFeatures` floats 48–55 (D-177); consumers: Ricercar (FL.4/FL.10) | **The four families are ORCHESTRAL and contain no guitar class — a structural limit, measured FTR.12.** Resolved AudioSet indices: `strings [189–194, 199]` = bowed string / string section / violin / pizzicato / cello / double bass / **harp**; `brass [185–188]`; `woodwinds [195–198]`; `percussion [161…182]` = the orchestral set anchored on Timpani. Guitar (`139` Plucked string, `140` Guitar, `141` Electric guitar, `143` Acoustic guitar, `144` Steel/slide, `146` Strum) is in **none** of them, so a rock-guitar route cannot be built on this series however it is scaled. The underlying model computes all 527 classes and the guitar ones do work on **clean, prominent** guitar (max-prob p50 **0.582 / 0.517** on solo classical guitar and clean jazz electric, against **0.096 / 0.006 / 0.004** on guitarless controls) — but on distorted rock guitar they read **0.071 / 0.086**, inside the guitarless range, and a dense player-piano recording false-positives at p95 0.390. **Exposing a guitar family would need its own increment and would serve clean guitar only.** Evidence: `docs/diagnostics/FTR12_GUITAR_CHANNEL_2026-08-12.md` §6. |
| **Continuous harmonic state (Tonal Interval Vector)** — `tonal_phase_fifths`, `tonal_phase_thirds`, `tonal_consonance`, `tonal_tension`, `harmonic_flux` | Supported (D-178; **liveness measured WL.1**) | `MIRPipeline.latestChroma` → weighted 12-point complex DFT → `FeatureVector`; consumers: Cymatic Resonance hue (CR.1.2 / D-198), Witchlight pen path (D-209, designed) | **Measured on four real captures at WL.1** (`WITCHLIGHT_DESIGN.md` §2): the two **phases are alive everywhere** — full ±π range on all four, including an 88-minute live session. They are **circular and noisy raw** (median per-frame step 0.31–1.04 rad): smooth `sin`/`cos` separately and recombine via `atan2` (τ ≈ 1.5 s), **never** EMA the raw ±π sawtooth. **The smoothed phase's angular rate varies ~10× across tracks** (0.91 vs 11.4 rad/s p95 at τ = 1.5 s) — a consumer that maps rate to motion needs explicit rate governance, not just a longer τ. **`harmonic_flux` and `tonal_tension` are alive on live captures (flux p50 0.058) but near-flat on 2 of 3 offline route-coverage fixtures (0.2 % nonzero on `love_rehab`)** — do not design an offline-testable route on them until that gap is understood (QG.1.1 class; open question, D-209). |
| Vocals pitch (YIN) | Supported | `PitchTracker` → `StemFeatures` floats 41–42 (MV-3c) | Used by Gossamer. **Duty measured WL.1:** nonzero on **4.5 %** of live-session frames and 2–3 % on two of three offline fixtures (41.8 % on the instrumental `so_what`, where the stem tracks the lead horn). Not eligible as a hero driver for any preset — garnish only, and only after measuring it alive on the target material. |
| **Circular-primitive rule (WL.2/WL.3, D-209)** | — | `tonal_phase_fifths`; falsified twice independently | **A circular primitive needs a DEVIATION treatment for the same reason a band energy does.** The smoothed phase is strongly *concentrated* on real music (mean resultant length up to 0.98), so steering geometry from its absolute value produces near-zero net motion. Use `wrap(circEMA(φ, τ_fast) − circEMA(φ, τ_slow))`, normalised per track against its own magnitude — D-026 applied to a circular quantity. Absolute reads stay correct for **hue** (CR.1.2 / D-198), where concentration is harmless. |
| **Absolute dynamics — `spectral_density` / `…_slow` / `spectral_surge`** (DYN.1 / .1b / .1c) | Supported | `SpectralAnalyzer` (+`+Density`) → `FeatureVector` floats 49–51; per-track band from `LoudnessProfile` (`Shared`) measured by `LoudnessProfile.measure` (`Audio`) at LF preparation, installed via `MIRPipeline.setLoudnessProfile`; `docs/ENGINE/DYN1_CALIBRATION.md` | **The only fields that survive normalisation** — computed from raw magnitudes, upstream of the total-energy AGC and `BandDeviationTracker`. Density = HF energy fraction (distortion adds harmonics, not amplitude); surge = pre-AGC level through an asymmetric follower, the one field that can express a STEP THAT PERSISTS. **DYN.1c: for LOCAL FILES the surge is this moment's RANK in the track's own loudness distribution** (33 quantiles from the full decode, persisted in `CachedTrackData` / PersistentStemCache **v7**); streaming keeps the fixed −24…−15 dB band, which pins for 63 % of a limited master and then cannot rise again (measured, Hummer; ranked it pins for 0.9 %). **Two edges are not enough and a fitted percentile is the same bug one level up** — see the calibration doc's mapping table before touching this. Continuous geometry may read the surge (slow); the fast density leg goes to a QUANTISED count, never to a length (FTR.3f). |
| Mood (valence / arousal) | Supported | `MoodClassifier` → `RenderPipeline.setMood` → `FeatureVector` | Smoothing happens in preset state; never write valence/arousal through `setFeatures` (Failed Approach #25). |
| Spectral history ring buffer (FFT-resolution trails) | Supported | `SpectralHistoryBuffer` (16 KB UMA at fragment buffer 5) | Used by SpectralCartograph; available to any direct-fragment preset. (Truchet Loom's `flux_smoothed`/`beat_index` reserved-slot additions were removed when it was retired — TLRETIRE.1 / D-194.) |
| Stem warmup blending for first ~10 s / ad-hoc mode | Supported | `smoothstep(0.02, 0.06, totalStemEnergy)` convention (D-019) | Mandatory for any preset reading `stems.*`. |
| Beat-grid-driven structural prediction (section boundaries) | Supported | `StructuralAnalyzer`, `NoveltyDetector` → `StructuralPrediction` | Consumed by the Orchestrator; also reaches the preset tick since Skein.ENGINE.3 (next row). |
| Live structural-section signal reaching the **preset tick** (CPU-only, gated, byte-identical) | Supported (Skein.ENGINE.3, D-151) | A separate lock-guarded store on `RenderPipeline` — `storedStructuralPrediction` + computed `latestStructuralPrediction` (default `.none`) + `setStructuralPrediction(_:)` (`RenderPipeline.swift` / `+PresetSwitching.swift`), mirroring the `setMood` value-injection bridge. Written from `VisualizerEngine+Audio.swift` at the per-frame MIR publish (right after `setFeatures`, reading `mir.latestStructuralPrediction`); read on the render thread by the Skein `meshPresetTick` closure (`VisualizerEngine+Presets.swift`) and passed to `SkeinState.tick(…structure:)`. Proof: `SkeinStructureSignalTests` (FA #66 — real bridge + the `RenderPipeline+Draw.swift:120` tick invocation indirection + ingestion + one-frame boundary flag). | **CPU-only** — NOT a `FeatureVector`/`Common.metal` GPU field (no D-099 migration), never written to a GPU buffer. **Byte-identical** for every other preset AND Skein itself: the setter defaults to `.none`, only `SkeinState` reads it, the `meshPresetTick` callback *type* is unchanged, and the stored signal lives in CPU-only fields `writeToGPU` never touches (`PresetRegressionTests` 20×3 + DB/FM MVWarp accumulation + `PresetLoaderCompileFailure` count all green). **Delivers + proves the signal only — the structural VISUAL is Skein.5** (infra-before-preset, FA #59/#60); the app is visually identical to today. |

| **Audible-EVENT marker (`spectral_level_rise`, FeatureVector float 53)** | Supported (FTR.24), **rate-invariance FIXED at FTR.24a / BUG-089** — fires ~0.40/s on BOTH analysis paths where the first formulation ran 0.89 vs 0.07. First consumer reverted (size); second consumer is Fractal Tree's tip spark (FTR.25) | `SpectralAnalyzer.advanceLevelRise` + the three constants in `SpectralAnalyzer+Density`, `SpectralAnalyzer+Result` for the field contract, `MIRPipeline.applyDensityFields`, `Common.metal` + `PresetLoader+Preamble` (THREE struct copies — the preamble one is what the loader compiles, and omitting it fails the shader compile and drops the preset silently), `SessionRecorder+CSV`, `AudioRoutePrimitives`, gate `LevelRiseTests` | Rise of the UNSMOOTHED pre-AGC level over a 0.15 s trailing MINIMUM, 4→12 dB smoothstep, instant attack / 0.20 s release. **The gap it fills was measured, not assumed: NO other `FeatureVector` field marks an audible event.** Event-versus-random specificity against 49 audible events — `spectral_surge` **0.25×** (it moves DOWN when the ear notices: it ranks a 0.76 s follower and a limited master dips in rank as a band arrives), `beatMid` 0.83× *below chance* (the `beat*` fields are pulse CLOCKS, not music), `spectralFlux` 1.50×, `bassDev` 1.75×; and through a real preset's composition every one of those DILUTED the base rather than sharpening it. ★★★ **Duty cycle, not gain, decides whether an accent IS one** — the first calibration was non-zero 75 % of the time, read as a DC lift, raised its consumer's floor 46 % and cost 25 % of its span (the FTR.16 "ambien" defect by a new route), and no consumer-side gain or weighting could repair it. Pair with `spectral_surge`, never substitute: same physical quantity, two timescales. ⚠ **Coverage hole:** near chance (1.2×) on dense limited electronica — the FTR.15 limiter mechanism at transient scale. ⚠ It is also the same detector as the criterion, so it DEFINES "audible event" rather than being validated against one. ⚠ QG.1 cannot verify a route on it until the fixture corpus is re-captured (`RouteCoverageTests.columnsPostdatingFixtures`). Evidence: `docs/diagnostics/FTR15_SIZE_READS_LEVEL_2026-08-13.md` §9. |
| **Event accent on LIGHT (brightness + desaturation, tip-weighted)** | Supported (FTR.25, first consumer Fractal Tree) | `FractalTree.metal` fragment stage — `spark = spectral_level_rise · depth_norm`, then `val += spark · 0.55` and `sat −= spark · 0.45`; gate `FractalTreeMeshRenderTest` accent A/B | **The place to put event marking after a scale property fails.** Nothing is positioned relative to brightness, so an accent here costs exactly ZERO peak velocity — which is why this works where the identical driver on SIZE multiplied peak velocity 10.7× and was rejected as defective (see the retired row below). Three rules, each one measured: (1) **weight linearly in depth, not squared** — squared attenuates the mid-canopy where most lit pixels are, and even gain 1.20 then moves the frame only 6 %; linear still holds the trunk at exactly zero, which is what keeps it off being a frame lift (a global flash was removed at FTR.3 under D-157 because no branch can read against a lifted frame); (2) **brightness alone sits at the threshold of visibility** (+5 % lit luma) — desaturating toward white doubles the pixel delta for the same flash budget, because that is what makes a small bright thing read as a SPARK; one gesture, one primitive, so FA #67 holds even where `sat` carries another route; (3) **gate the premise, not the look** — assert the canopy width is identical TO THE PIXEL across the accent while lit luma moves, or the term has silently leaked into geometry. ⚠ Regime dependence: this is the opposite of Floret, whose drum sparkle was removed because bright points camouflage in a BRIGHT field. Sparse and dark is where points read. Evidence: `docs/diagnostics/FTR15_SIZE_READS_LEVEL_2026-08-13.md` §11. |
| **Phase-driven GAIT (a visual that dances rather than reacts)** | Supported (FTR.28/29, first consumer Fractal Tree) | `Geometry/DancePhase` (render-rate PLL) + `MeshGenerator.applyDanceClocks` + the root-lean/spring and per-level lagged terms in `FractalTree.metal`; gates `gaitOscillatesAtTheClockPeriod` (controlled) and `danceIsBeatPeriodic` (real capture) | **The answer when a preset reads as unconnected while every amplitude route measures healthy.** Nine live rejections in FTR.15→FTR.27 were spent on WHICH SIGNAL sets a size; the ask was a DANCE, and a dance is a phase — a gait locked to the pulse, with intensity setting only step size. Four rules, each one measured: (1) **the clock must be render-rate** — `beatPhase01` arrives at 14.6 Hz in 0.109-beat steps and fed raw to geometry the rendered period is 0.133 s, the update cadence; (2) **the clock must rate itself** from dφ/dt, not from `BeatHold`, which vouches on 0–13 % of frames (BUG-096) — gated on it the lean scored +0.293 against a +0.285 decoy, self-rated +0.757 against +0.222; (3) **ONE CLOCK** — amplitude signals may set how big the motion is but must not add motion at their own rate: with the canopy angle on live flux and the gait's gain on live `bass_dev`, only **22 %** of the tree's travel was on the music and Matt called it *"a very bad dancer"*; putting both on slow glides plus a 6 s growth arc took that to **81 % at a third of the total motion**; (4) **act at the ROOT as well as per level** — a gait applied only inside a branch walk that advances position before rotating moves the body 0.3 px, because the first segment out of the root never tilts. Instrument note: autocorrelation cannot find the gait under tip churn — use a matched filter against the recorded phases with a time-reversed decoy, and regress the rendered pose on the clock basis for a coordination R². Evidence: `docs/diagnostics/FTR15_SIZE_READS_LEVEL_2026-08-13.md` §§8–11 + the FTR.28/29 plan entries. |
| **HELD-AND-STEPPED magnitude (a size that holds still and changes on an arrival)** | Supported (FTR.33, first consumer Fractal Tree) | `Geometry/ArrivalStep` (CPU) + `MeshGenerator.bindGrowthStep` → object/mesh **buffer(8)**; consumer `fractal_growth` in `FractalTree.metal`; gate `ArrivalStepTests` (10 tests) | **The answer when a magnitude route reads as random while measuring as accurate.** Fractal Tree's size followed true loudness from `raw_tap.wav` at **r = +0.863** and was rejected as *"seemingly random"* on three consecutive reviews — so accuracy is not the property that makes a channel legible. What makes one legible is a reference the LISTENER also holds: the beat, in the gait row above; a sound LANDING, here. Shape: a few discrete tiers on a **per-track RANK**, hysteresis + minimum dwell on the edges, each change committed on a `spectral_level_rise` arrival (with a timeout so a gradual build still grows), a cooldown so two tiers walk instead of jumping, and one ease onto the new tier. Measured on a real capture: **88 % of frames perfectly still**, travel 0.042/s against the drifting version's 2.03/s, and **42 %** of changes within 0.35 s of an audible arrival against **24 %** matched-random and **17 %** for an arrivals-shuffled null. Four rules, each learned from a failure: (1) **edges belong on a rank, not on a value** — fixed edges on the level left the tree in one tier for 130 of 142 s, and a running mean-and-spread seeded from one sample read 0.691 against a level of 0.445 and never stepped at all; use DYN.2c's `spectral_section_ratio`, which is uniform over the track by construction, and there is no filter to seed; (2) **a matching p05 and p95 do not make a matching visual** — the tier DISTRIBUTION is what the eye integrates, and a calibration that matched both percentiles still rendered a visibly smaller tree that had lost a whole generation of branches; (3) **the arrival is a TRIGGER, never a magnitude** — the same field used as a continuous accent on the same property multiplied peak velocity 10.7× and was rejected on sight (the retired row below); (4) **advance the state on undrawn frames too**, or the tier depends on the draw cadence — this is the third increment to hit that trap (FTR.18, FTR.30), and here it rendered a small, still tree that looked exactly like a calibration error. Do not pair with a continuous term on the same property: Fractal Tree's density `lift` was deleted because it would have kept the count wandering underneath the steps. |
| Two-layer visual magnitude (slow base + fast decaying accent on ONE visual property) | ❌ **RETIRED at FTR.24a — tried on Fractal Tree's size and measured as unworkable there** | `FractalTree.metal` — `saturate(base + gain · accent · (1 − base))`, accent read LIVE from buffer(0) inside the beat-held evaluation | The pattern for "it must track the section AND mark the moment". **Event alignment needs a fast response, low travel needs smoothing, and one term on one layer cannot do both** — seven consecutive live rejections of one complaint were that, and six single-term reformulations each traded one half for the other. Scaling the accent into the base's REMAINING HEADROOM is what keeps it from pinning (0 % of frames, where additive forms pinned 11–15 %). Keep it off quantised terms (a branch count pops); keep it off the beat-held vector (an accent that latches to the grid cannot mark an event). ❌ **DO NOT REACH FOR THIS ON A SIZE/SCALE PROPERTY.** Measured across five builds on Fractal Tree: every gain that marks 0.8–1.5 events/s multiplies peak velocity (up to **10.7×**, which Matt called *"herky-jerky … looks defective"*), and every gain that does not is **below chance** on event alignment (0.81× while still costing 2.4× peak velocity). **Event alignment and calm motion are ANTI-CORRELATED on a scale property**, because scale is what everything else is drawn relative to. Two further traps if it is ever attempted elsewhere: an accent added AFTER a render-rate glide is the one driver with NO smoothing (FTR.14's whole lesson, re-learned the hard way), and an event accent is incompatible with a per-beat HOLD contract on the same property (1.80 turns/beat against FTR.10's 0.38 bar). The property to accent is one where a fast change displaces no geometry — colour value, brightness, tip flicker. Evidence: `docs/diagnostics/FTR15_SIZE_READS_LEVEL_2026-08-13.md` §10. |

## 8. Visual harness / tooling capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Per-preset golden dHash regression | Supported | `PresetRegressionTests.swift` (Increment 5.3, D-039) | 64-bit dHash, Hamming ≤ 8 tolerance. |
| **Offline per-stem feature measurement over an ARBITRARY local file, with controls** | Supported (FTR.12, 2026-08-12) | `GuitarChannelReportTests.swift` (`FTR12_AUDIO=…` + `--filter GuitarChannel`); corpus runner `Scripts/ftr12_guitar_channel.sh`; findings `docs/diagnostics/FTR12_GUITAR_CHANNEL_2026-08-12.md` | Decode → `StemSeparator.separate` in the model's fixed ~10 s window → `StemAnalyzer.analyze` per 1024-hop (`SessionPreparer.warmUpAndAnalyze`'s exact framing, every frame kept) → r-against-drums, p05/p50/p95, distinct-value count and CHR.1's common-mode share for 4 stems × 4 features, plus IFC.4 families and a direct PANNs class probe. **Why it is not capture replay:** the recorded sessions are a handful of tracks Matt happened to play, so they cannot answer whether a feature *generalises* — offline selection costs no live-session time and is the only way to include **negative controls** (material where the answer is known to be "absent"). That is the reusable part: any "does feature X track instrument Y" question should be run this way, controls first (`[[feedback_control_stem_before_building_driver]]`). **Lag-immune by construction** — stem-vs-stem comparisons cancel BUG-086's ≈2.9 s latency; anything compared against a non-stem series needs an explicit lag sweep instead. Knobs: `FTR12_SECONDS`, `FTR12_FPS` (the fps handed to `analyze`, i.e. every internal EMA's τ — used to show the FTR.12 verdict is not an offline-rate artifact), `FTR12_SKIP_PANNS`. Corpus paths are hard-coded to `/Volumes/Extreme SSD` (override `FTR12_LIB`) and the harness no-ops without `FTR12_AUDIO`, so a normal suite run skips it. |
| Per-preset acceptance invariants (silence non-black, beat ratio, form complexity) | Supported | `PresetAcceptanceTests.swift` (Increment 5.2, D-037) | |
| Final-frame visual review harness (PNG export at 1920×1280, multi-fixture contact sheet) | Supported | `PresetVisualReviewTests.swift` (Increment V.7.6.1; BUG-034 2026-06-12; ROOTCHOIR.1) | Gated by `RENDER_VISUAL=1`; `RENDER_PRESET=<name>` limits the argument sweep. BUG-034: the deferred ray-march path binds production-parity inputs and covers all five ray-march presets. ROOTCHOIR.1 adds direct-preset slot-6 state parity and selects five named review states from the committed So What real-music capture (rest, stable harmony, harmonic movement, tension, bass). Remaining generic fixture deltas by design: zero stems, audioTime 0, no D-022 valence tint. `RENDER_STEP_MULT=<float>` overrides the D-057 multiplier for step-budget A/B pairs. |
| Visual review harness for **mesh-shader** presets | Supported (FTR.2) | `FractalTreeMeshRenderTest.swift` | `PresetVisualReviewTests.renderPresetVisualReview` refuses mesh presets outright (`guard !preset.descriptor.passes.contains(.meshShader)`), so until FTR.2 there was **no way to render a mesh preset from a test** and the checklist's mandatory pre-commit contact sheet was unreachable for them. Drives the real object → mesh → fragment dispatch through `MeshGenerator(device:pipelineState:configuration:)` + `draw(encoder:features:)` into an offscreen target; writes per-frame PNGs plus a horizontal contact sheet under `RENDER_VISUAL=1`. Drive frames come from the bundled `route_coverage` fixtures sampled at percentiles of the primitive under review (FA #27 — real music, not hand-authored envelopes), plus an all-zero frame for D-037. Metrics printed every run: mean luma, ink fraction, canopy width, circular-mean hue, p05→p95 mean \|Δpixel\|. **Currently Fractal-Tree-specific** — the drive-frame builder names the fields that preset reads. **FTR.6:** engine-derived fields are now RECOMPUTED through their production object rather than read from the CSV (`MelodicNoteGate` for `melodic_tips`, joining the existing `SpectralAnalyzer` density recompute) — a capture holds whatever the build that recorded it produced, and every session predating a new field would pin its route to 0 and report a live hero layer dead. Generalising it is the obvious next step for any second mesh preset. Stops short of `RenderPipeline.drawWithMeshShader` (which also binds StemFeatures at fragment buffer(3) and noise at 4–8); Fractal Tree reads none of those. |
| **Refractory-gated note events — `melodic_tips`** (FTR.6) | Supported | `Sources/DSP/MelodicNoteGate.swift` → `FeatureVector` float 53; consumed by `FractalTree.metal` | **The first `FeatureVector` field whose value depends on WHEN the previous event was, not only on this frame's spectrum.** Level trigger on `beat_mid` (0.90 — the pulse clips at 1.0, so any level in (0.75, 1.0] selects the same full-strength beats) plus a refractory of one eighth note derived from `stableBPM`, feeding a unit accumulator drained at τ 2.5 s. Truncating it (`(uint)f.melodic_tips`) steps by exactly ONE unit. **Use it wherever a preset needs a countable per-note event rather than a continuous pulse** — no stateless shader transform of `beat_mid` can produce one, because the rate plateaus at ~6.5/s and the shader has no memory of the last event (measured, FTR.5-prep). Measured 2.92 / 2.87 events/s on *Hummer* / *Cherub Rock* against a 3.29/s guitar note rate. **Two limits before you route it:** it does NOT separate instruments (mid band is snare AND guitar, +0.973 correlated on rock material — see §MEL.1), and a consumer that lets the count fall back sees ~2× the event rate in total transitions, because each unit must also leave. Tuning constants and the sweeps behind them are in the file's header. |
| **Mood-classifier input calibration probe** (DYN.6) | Supported | `MoodFluxCalibrationTests` (`MOOD_AUDIO=…`); `MoodClassifier.scalerMeansForTesting` / `.scalerStdsForTesting` | Reports each of the ten mood features as a z-score against the SHIPPED scaler, at both the offline-preparation and live analysis rates, through the production `MIRPipeline`. **Asserts rate-invariance only** (< 0.25 σ between rates) — where features sit relative to TRAINING is reported, never asserted, because a test cannot decide whether a distribution shift is a defect or the music. **Settled at DYN.6.1 by the corpus census (n = 27,638):** nine features sit at z(median) −0.45…+0.21 with ≤ 4.7 % beyond |z|>2; **flux sits at +1.01 with 33.8 % beyond |z|>2**. The scaler is NOT stale — it matches the annotated training set (n = 818) to five decimals. The gap is training-set COVERAGE: that set's maximum flux is 1.0167 and **15.3 % of the corpus exceeds it entirely**. Consistent with the +1.43 residual recorded and signed off at BUG-066. **Remedied at DYN.6.2 (Matt's call):** flux refitted to corpus statistics (mean 0.56498, std 0.43400); the other nine features keep their training fit. Accepted on an objective 21,037-track A/B (`MoodScalerRefitABTests`) — arousal mean +0.378 → +0.229 with spread intact, railed readings 0.5 % → 0.0 %, 41.3 % of tracks changing quadrant. **The A/B shows the change is real and bounded, NOT that it is right** — the corpus is unlabelled, and valence spread narrowed (0.507 → 0.422). One constant pair to revert if mood-driven selection reads worse in use. |
| **Rate-invariant density smoothing** (DYN.4) | Supported | `LoudnessProfile.emaAlpha(deltaTime:tau:)` + `tau(legacyAlpha:)`; `SpectralAnalyzer.process(magnitudes:deltaTime:)`; `DensitySmoothingRateInvarianceTests` | Every density follower was an EMA with a per-FRAME alpha, so its width was `1/(α·fps)` — calibrated at 43.07 Hz while **the live pipeline runs at 59.9 Hz** (measured, `2026-08-10T01-29-10Z`): section τ 20 s → 14.4 s, normal τ 45 s → 32.1 s. `LoudnessProfile.measure` builds its quantiles from a τ20 s series, so ranking a τ14.4 s signal against them compressed the live `spectral_section_ratio` to 0.534…0.614 of 0…2. Widths are now seconds, converted per frame; each τ reproduces its legacy coefficient exactly at the reference rate, so nothing is retuned. **Any new follower must take `deltaTime` — a per-frame alpha is a latent rate dependency, invisible to any test that runs at one rate.** DYN.5 extended this to the centroid / rolloff / flux followers and the flux running-max decay, so no per-frame constant remains in the spectral path. **The DYN.5 "known residual" was RETRACTED at DYN.6.** It claimed `rawSmoothedFlux` stays rate-dependent because it is a per-frame difference — true of the synthetic drive that produced it, false of real music, where a 1024-sample window decorrelates almost completely between hops. Measured through the production pipeline on eight tracks, **every one of the ten mood-classifier features shifts by < 0.11 σ between the 43 Hz offline rate and the 59.9 Hz live rate** (`MoodFluxCalibrationTests`, DYN.6). Rate-invariance is end-to-end. |
| **Canopy data-path probe — which source `spectral_section_ratio` came from** (DYN.3) | Supported | `MIRPipeline.updateCanopyProbe` → `DENSITY_PATH:` log line + `canopyDensityBranch` / `canopyAnalysisFPS`; `CanopyDensityPathTests` | `spectral_section_ratio` has two sources — DYN.2c's ranked branch (uniform over the track, spans 0…2) and DYN.2b's live-EMA fallback (a ratio of two EMAs of one signal, so it sits near 1.0 by construction) — and **one recorded column cannot distinguish them after the fact.** Fractal Tree's whole canopy reads this field, and on `2026-08-07T22-59-38Z` it spanned 0.785…1.084 live where the same audio and the same cached profile span 0…2 offline. The probe names the branch once per track and records the **measured analysis fps**, which nothing previously did: every density leg uses a per-FRAME alpha, so its time constant is `1/(α·fps)`, and a live rate other than the ~43 fps the constants were calibrated at silently retunes all four legs. Five tests gate the classifier — a diagnostic naming the wrong branch is worse than none. **Reusable by any preset reading a profile-backed field.** |
| Response-band gate (QG.5) reachable from a **mesh** preset | **Missing** | `ResponseBandTests.makeRuntime`, `AudioResponseMetrics` | QG.5 gates the VISUAL response by driving a Swift runtime conforming to `AudioResponseMetrics` over a fixture replay and reading `responseMetric(_:)` — Witchlight has `WitchlightPath`. A mesh preset computes its visual quantities **in the object/mesh shader on the GPU** and may have no Swift-side code at all (Fractal Tree is `FractalTree.metal` + `FractalTree.json`, nothing else), so there is no object to register and no metric to publish. Declaring a band anyway is a hard failure by design ("a band nobody can measure is worse than no band"). Closing this needs either a CPU mirror of the shader's parameter math or GPU readback of mesh output — **its own infrastructure increment**, serving every mesh preset. Until then mesh presets discharge the obligation as a measured swing table in the closeout (FTR.2 precedent). Corrects the FTR.1-era claim that QG.5 did not exist. |
| Pass-separated capture (e.g. WORLD-only, WEB-only, BG-only) | Supported (staged presets) | V.ENGINE.1: `PresetVisualReviewTests.renderStagedPresetPerStage` writes one PNG per stage per fixture under `RENDER_VISUAL=1`; `RENDER_STAGE=<name>` filters to a single stage. Headless render-through is also covered by `StagedCompositionTests` so the path is exercised on every `swift test` run. | Available only for presets that adopt `RenderPass.staged`. mv_warp / ray-march presets remain final-frame-only at the harness level (their internal passes do not surface as named stages). |
| Shared multi-frame harness spine (env-gate, silence buffers, capture texture + clear + readback, silence FeatureVector, non-degeneracy / luma / dHash metric hooks) | Supported | `HarnessTemplateCore.swift` (QG.4, D-182). `AuroraVeilMVWarpAccumulationTest` is re-based on it (byte-identical assertions). | `HARNESS_TEMPLATES=1` env gate; dHash byte-identical to `ArachneSpiderRenderTests` so goldens compare across the suite. |
| Multi-frame harness template — `mv_warp` paradigm (scene → warp → compose → swap, N frames at silence, accumulator capture + metric) | Supported | `AuroraVeilMVWarpAccumulationTest.swift` (AV.2.x; re-based on `HarnessTemplateCore` at QG.4/D-182) | The reference pattern named in `PRESET_SESSION_CHECKLIST.md` Part 2 obligation 2. |
| Multi-frame harness template — `mv_warp` **`strandsOnTop`/SDF-in-fragment marks-on-top** sub-paradigm (Dragon Bloom/Skein's pattern: warp → geometry-overlay-drawn-directly-onto-the-warped-texture → swap), for an **unregistered scratch preset** (no sidecar in `Sources/Presets/Shaders/`, no `expectedProductionPresetCount` change) | Supported (WHIT.0, 2026-08-25) | `RosetteLookSpikeTests.swift`; real `PresetLoader(watchDirectory:loadBuiltIn:false)` compile path (the `PresetLoaderTests.swift` scratch-dir pattern), hand-driven `warp`/geometry-overlay encoders matching `RenderPipeline+MVWarp.swift`'s `drawWithMVWarpStandard` call-for-call | Distinct from the row above: `AuroraVeilMVWarpAccumulationTest` exercises the non-`strandsOnTop` scene+decayed-compose branch on an already-shipped preset; this exercises the `strandsOnTop` branch (`RenderPipeline+MVWarp.swift:138`) on a preset that is **never added to the shipped bundle** — the mechanism for a throwaway look-spike to reach the real dispatch path. **Found live, fixed in the harness (not engine source):** `mvWarp_fragment`'s fragment `buffer(0)` is `chromaticMix`, distinct from the vertex-table `FeatureVector` at the same index — leaving it unbound reads undefined GPU memory and renders as a runaway hue-zoom kaleidoscope artifact. `AuroraVeilMVWarpAccumulationTest`'s own `encodeWarp` has the same unbound gap, latent/unnoticed there (follow-up filed, not yet fixed). Also found: `drawSceneGeometryOverlay` binds `FeatureVector` at the VERTEX argument table only — a geometry fragment needing per-frame scalars (clock, audio) must receive them via the interpolated vertex→fragment struct or a dedicated per-preset buffer (Skein's `buffer(6)` convention), never by re-declaring `FeatureVector` as a fragment parameter. |
| Rendered-motion measurement for an audio route (does more drive actually LOOK like more?) | Supported | ALFVEN.3e: `ALFVEN_VIGOUR=1` in `AlfvenFilmPreviewTests` — mean per-frame absolute pixel delta on the PRODUCTION display path at a held `bassRel`, with `ALFVEN_DRIVEFLOOR`/`ALFVEN_DRIVECEIL` pinning forcing to one value so the drive axis can be swept directly. Deterministic: repeat runs are bit-identical, so any difference is signal. | Answers the question a route-firing manifest cannot: `RouteCoverageTests` proves a primitive MOVES, not that the render responds to it. Alfvén's drive route was green on the manifest while its top 5 % rendered identically. ⚠ Measure at steady state — the default window is frames 600–900 because a 240-frame run is still energising and reports a linear response where the steady-state one is flat. ⚠ It measures whole-frame change, so it cannot separate stirring from palette drift or bloom; hold the other routes constant. |
| Multi-frame harness template — PERSISTENT `staged` paradigm (60 frames through `encodeOffscreenStages`, persistent state captured every frame) | Supported | `PersistentStagedPathHarnessTemplate.swift` (ALFVEN.1); subject Poisson Sandbox (velocity → divergence → pressure[persistent, 24 sweeps, rgba32Float] → project → compose) | `HARNESS_TEMPLATES=1`; env-gated. Metric is the state field's RMS trajectory, asserting BOTH failure directions a stateful solver has: it must not saturate (bounded, per-frame growth decelerating, watchdog never trips) and must not decay to zero. Note it asserts *converging*, not *converged* — at 256² the field is still climbing at frame 60, and a "has settled" assertion would fail a healthy field. |
| Multi-frame harness template — `staged` paradigm (all stages via production `encodeStage`, slot-6/7 bindings; per-stage non-degenerate + composite dHash golden) | Supported | `StagedPathHarnessTemplate.swift` (QG.4, D-182); subject Arachne (world → composite) | `HARNESS_TEMPLATES=1`; env-gated, not in the default parallel run. A/B validated: unbinding the sampled world input (slot 13) → constant-dark composite fails it. |
| Multi-frame harness template — `ray_march` paradigm (G-buffer → lighting → composite → post via live `RayMarchPipeline.render`; non-degenerate + composite dHash golden) | Supported | `RayMarchPathHarnessTemplate.swift` (QG.4, D-182); subject Lumen Mosaic | `HARNESS_TEMPLATES=1`. A/B validated: unbinding the slot-8 follower → BUG-016 black cells (meanLuma 0.47→0.15), golden trips. |
| Multi-frame harness template — `feedback` paradigm (surface-mode `runWarpPass` → composite → swap; accumulator held between the D-037 non-black floor and saturation) | Supported | `FeedbackPathHarnessTemplate.swift` (QG.4, D-182); subject Membrane | `HARNESS_TEMPLATES=1`. A/B validated: skipping the compose pass → accumulator decays to 0.0, floor fails. |
| Anti-reference rejection gate (final frame must not Hamming-match a known anti-reference) | Missing | — | Anti-references exist as JPEGs (`09_anti_clipart_symmetry.jpg`, `10_anti_neon_stylized_glow.jpg`). No automated check. |
| Render-vs-reference comparison sheet (side-by-side compositor for the mandatory pre-commit perception check) | Supported | `Scripts/compare_render.sh` + `Scripts/compare_render_composite.swift` (QG.2, D-181) | Composites the newest `RENDER_VISUAL=1` frames for a preset against every image in `docs/VISUAL_REFERENCES/<preset>/` into one sheet (reference left, render frames right, filenames burned in); prints the path, non-zero exit on missing inputs. Repo-native `swift` compositor (no ImageMagick on the dev box). **Reader-is-the-eyes: no auto-scoring** (D-064). Mandated by `PRESET_SESSION_CHECKLIST.md` Part 1 step 5; `closeout_evidence.sh` surfaces the sheet path. |
| Motion review gate (frame-to-frame magnitude signal + sampled-sequence viewing for the pre-M7 temporal check) | Supported | `Scripts/motion_gate.sh` (PG.MG, D-195); `PresetVisualReviewTests.renderRootChoirMotionSequence` (ROOTCHOIR.1) | The temporal counterpart to the D-181 still sheet. ffmpeg computes mean-luma of each consecutive-frame difference → spike count (`>3× median`) flags jitter, ~0 flags freeze; ~8 frames are staged for reader review. **Reader-is-the-eyes: no auto-pass** (D-064). ROOTCHOIR.1 lands the first preset-specific contiguous sequence feed: all So What capture rows tick the real CPU phase state and every third row renders (~14 fps) under `RENDER_ROOT_CHOIR_SEQUENCE=1`. New presets may follow that production-state + real-row pattern; no target animation exists yet for Root Choir. |
| Audio-visual coupling report (cross-correlate per-frame visual delta vs. energy envelope) | Supported | `CouplingReportTests.swift` + `MultiPassRenderHarness.swift` (QG.3/QG.3.1, D-182/D-183); baseline `docs/diagnostics/QG3_COUPLING_BASELINE.md` | Per-frame visual delta (mean-abs-luma over a downsampled field) cross-correlated (peak Pearson r, lag 0–500 ms, 10 s-window stationarity) against composite/band energy + a per-preset mismatched-pair noise-floor control. **Report-only, never a gate** (D-182). QG.3.1: rendered through each preset's REAL path — the 10 multi-pass/feedback/follower presets via the shared `MultiPassRenderHarness` (the flash-gate seam, feedback persistence), the 3 single-pass presets via one fragment + Nimbus follower. **All 13 measurable** (was 2); 11/13 clear their per-preset floor; the floor is per-preset (feedback autocorrelation), not global. Weak-reading Nacre/Ferrofluid = proxy/render-fidelity limits, not defects. QG.3.2 gate = warning tier (D-183). |
| Reference image completeness lint (preset folder has README + N images, names match `_NAMING_CONVENTION.md`) | Supported | `swift run --package-path UzumeTools CheckVisualReferences` (Increment V.5, D-064) | |
| Automated rubric evaluator (M1 cascade, M2 octaves, M3 materials, M4 deviation, M5 silence, M6 perf, M7 manual; E1–E4; P1–P4) | Supported | `Sources/Presets/Certification/FidelityRubric.swift`; `PresetCertificationStore` (Increment V.6) | |
| Quality reel orchestrator | Partial | `docs/quality_reel.mp4` exists; recording workflow documented | No automated reel-record harness yet. |
| Per-pass timing breakdown (engine-level) | Partial | `FrameTimingReporter` (cumulative + rolling); `FrameBudgetManager` | Frame-level only; no per-pass attribution. |

## 9. Performance / certification capabilities

| Capability | Status | Evidence | Notes |
|---|---|---|---|
| Frame budget governor (quality ladder, asymmetric hysteresis, per-tier config, thermal/LPM floor) | Supported (ladder lost its `.noSSGI` rung at RECON.18; Low Power Mode floors at `.noBloom` since RECON.19 — D-167 amended) | `FrameBudgetManager.swift` (Increment 6.2, D-057; thermal/LPM floor CLEAN.4.6 / D-167) | Six levels: full → noSSGI → noBloom → reducedRayMarch → reducedParticles → reducedMesh. CLEAN.4.6: a `thermalFloor` (applied level = `max(timing, floor)`) driven by `ProcessInfo` thermal state + Low Power Mode pre-empts the GPU's own throttle (`VisualizerEngine` listener → pure `qualityFloor` map; serious→no-bloom, critical→step-0.75, LPM→≥no-SSGI). Ultra/recording exempt; device validation of the live thermal response pending. |
| Quality ceiling override (Auto / Performance / Balanced / Ultra) | Supported | `QualityCeiling` enum; `SettingsStore.qualityCeiling` (Increment U.8) | Ultra exempts the governor. |
| ML dispatch gating (defer stem separation when frames are over budget) | Supported | `MLDispatchScheduler.swift` (Increment 6.3, D-059) | Tier-aware; max defer 2 s / 1.5 s. |
| Memory reporter (`phys_footprint`, matches Activity Monitor) | Supported | `Sources/Diagnostics/MemoryReporter.swift` (Increment 7.1, D-060) | |
| Frame timing histogram + rolling buffer | Supported | `Sources/Diagnostics/FrameTimingReporter.swift` | 100-bucket 0.5 ms histogram + 1000-frame rolling window. |
| Soak test harness (headless JSON+Markdown report) + peak-RSS leak regression gate | Supported | `Sources/Diagnostics/SoakTestHarness.swift` + `SoakRunner` CLI (2 h runs); `MemorySoakGateTests` (CLEAN.4.7 — frame-count `phys_footprint` growth gate) | `Scripts/run_soak_test.sh` for the manual 2-hour growth curve. `MemorySoakGateTests` (FFT→MIR + StemAnalyzer soaks, < 25 MB growth after warmup) catches gross leaks — run isolated via `SOAK_TESTS=1 swift test --filter MemorySoakGate` (`phys_footprint` is process-wide, so the parallel `swift test`/closeout skips it). |
| Display hot-plug / capture-mode switch resilience | Supported | `DisplayChangeCoordinator`, `CaptureModeSwitchCoordinator`, `NetworkRecoveryCoordinator` (Increment 7.2, D-061) | |
| Per-preset complexity cost gate (per device tier) | Supported | `ComplexityCost`; `DefaultPresetScorer` exclusion (Increment 4.0) | |
| **Ray-march render scale (`render_scale`)** | Supported (their PERF.11, 2026-08-20) | `PresetDescriptor.rayMarchRenderScale` → `RayMarchPipeline.renderScale`; first consumer Volumetric Lithograph at 0.5 | The lever for a ray-marcher that is expensive by construction rather than by waste. G-buffer and lighting allocate at `scale × drawable` and the composite pass upscales **for free** — it already samples `litTexture` by UV through a linear sampler — so there is **no new pass, no motion vectors and no MetalFX**, which matters because MetalFX Temporal would need `scenePrevPosition` and no preset has ever defined it (VL's terrain morphs, so the delta is not representable and the failure mode is ghosting, worse than softness). Post-process stays at drawable size so bloom and ACES run full-res on the upscaled image. ★ `effectiveRenderScale` had existed since MFX.1 (2026-07-23) gated on `usesMetalFXTemporal`; the two were separable, and **a parallel session simultaneously built a second mechanism for this without finding it** — grep the descriptor before adding a render-scale field. (The MetalFX-gated pair was deleted with MFX.1 at D-213/RECON.14; `render_scale` → `rayMarchRenderScale` is the sole mechanism now.) Measured on VL at 1080p: **31.9 → 8.65 ms with the harness readback removed** (17.43 ms with it on). ⚠ Live is higher — the harness is 2.4× low on VL — and **4K/60 remains out of reach** (~70 ms at 0.5). Visible change to a certified preset: needs an M7. |
| **Absolute frame-cost gate + strict 60 fps check** | Supported (PERF.12) | `PresetFrameBudgetTests.absoluteCeilingMs` (60 ms, always on) and `FRAME_BUDGET_STRICT=1` (16.7 ms, isolation-only) | The ratio gate (8× median) catches an outlier but **cannot see a whole-roster miss** — VL sat at ~31 fps at 1080p reading green at 5.9× median, and the `absoluteCeilingMs` the file's header described had never been implemented. ⚠ **An absolute ms gate cannot live in the default suite at the real budget:** parallel `swift test` inflates timings 2–3×, so 16.7 ms would fail in CI and pass locally. Hence two nets: a loose always-on 60 ms ("arriving already broken" — original Witchlight 273.9 ms, original VL 111.5 ms), and the true promise behind an env flag to be run in isolation. **Time with `readback: false`** — the harness's CPU readback costs a median 11.4 ms/frame at 4K and production never does it; timing with it on is what produced BUG-099's phantom "~30 fps". Strict now passes 20/20 at 1080p and at 4K. |
| **Offline DIRECT-pass render for frame-cost measurement** | Supported (PERF.10; stateful direct added ROOTCHOIR.1) | `MultiPassRenderHarness.renderDirectPreset`; consumers Nebula / Plasma / Spectral Cartograph / Waveform / Root Choir in `PresetFrameBudgetTests` | One generic path for fullscreen direct fragments. ★ **Reproduce EVERY binding the real call site makes** (`RenderPipeline.encodePresetVisualization`): FeatureVector 0, FFT 1, waveform 2, StemFeatures 3, spectral history 5, real generated noise textures, and any preset-owned state. ROOTCHOIR.1 proves the stateful variant by ticking `RootChoirState` and binding its actual slot-6 buffer on every frame; omitting it would time a cheaper, frozen picture. Feed spectrum buffers dense deterministic content. Root Choir's ROOTCHOIR.2 isolated no-readback 1920×1080 result: **3.55 ms/frame best of three passes**, under SHADER_CRAFT's 7 ms Tier-2 preset-work ceiling. |
| **Engine-level GPU binding proof via a runtime-compiled probe shader** | Supported (PERF.10) | `MeshStemBindingTests` — `device.makeLibrary(source:)` over `PresetLoader.shaderPreamble` + an embedded MSL probe; gates object-stage `StemFeatures` at buffer(3) and buffer(5) | **The pattern for proving an ENGINE binding works when no preset currently consumes it.** FTR.4's original gate rendered Fractal Tree twice and required the pixels to differ; FTR.33 moved that preset's tips off the stems and Fractal Tree is the only mesh preset in the repo, so the gate lost its consumer and started failing by correctly reporting a route removed on purpose. Deleting it would have left the next mesh preset's stem route free to ship dead. A probe shader compiled at runtime has no `.metal` file, no bundle resource, no `Package.swift` change, and **no preset can retire it by changing its own routing** — and compiling against the real preamble pins that struct layout for free. Reach for this whenever a gate is red because its consumer went away rather than because the engine broke. Validated by negative control (probe pointed at buffer(4) → red, restored → green). |
| **Offline MESH-pipeline render for frame-cost measurement** | Supported (PERF.7) | `MultiPassRenderHarness.renderMeshPreset` → `PresetLoader` + `MeshGenerator` (object → mesh → fragment); consumer `PresetFrameBudgetTests`; gate `fractalTreeIsMeasuredAlive` | The harness's sixteenth path and its first non-fragment one. Two rules a future mesh row must follow. (1) **Pin the render delta** (`renderDeltaOverride = 1/60`): `MeshGenerator.nextRenderDelta` reads the wall clock, so glides and `DancePhase` would advance at a rate set by how contended the timing machine is — timing geometry that depends on how fast it is being timed is circular. (2) ★★ **Check the preset's silence GATE before recording any baseline.** `PresetFrameBudgetTests.drive` leaves `pulseAmp01` at 0, which is Fractal Tree's gate, and a naive addition timed the 7-branch silent figure — a real number for a state no listener ever sees, green forever while the canopy got arbitrarily expensive. `openTheGates` tops the drive up per preset. ★ And assert the outcome on an observable that can actually see it: whole-frame ink separates playing from silent only **1.4×** (the trunk and first two generations are in both and dominate the count), while upper-canopy ink separates them **3.9×** (1175 vs 302 at 640×360). Measured cost: Fractal Tree 3.88 ms standalone / 5.88 ms in-suite at 1920×1080, 0.6–0.7× the median. **Coverage 16 of 29; the other 13 are surveyed in `uncoveredPresets` and none is a mesh preset**, so this path unlocks exactly one — `direct` (Nebula, Plasma, Spectral Cartograph, Waveform) is the cheapest next four. |
| **Offline `mv_warp` + bespoke-state render for frame-cost measurement — Gossamer** | Supported (PR.18) | `MultiPassRenderHarness.renderMVWarp` (`"Gossamer"` case) + `warmGossamer`; consumer `PresetFrameBudgetTests`; gate `gossamerIsMeasuredWithWavesAlive` | The route `uncoveredPresets` had predicted: the GENERIC `mv_warp` path plus `GossamerState` bound at fragment slot 6 and ticked once per frame — the same three lines `bindGossamerRuntime` performs in the app, and the shape Skein established. ★★ **The state was warmed, for PERF.17's reason:** `gossamer_fragment` loops `wave_count` and evaluates a Gaussian ring per wave per fragment, so the pool size IS the variable cost; a fresh `GossamerState` holds **2** seeded ambient waves and 24 timed frames buy 0.4 s against a 0.5–2.5 waves/s emission rate. `warmGossamer` runs 10 s of drive — past `maxWaveLifetime` (6 s), so the pool settles wherever the DRIVE puts it (**6 of 32**) rather than at a target count that would be manufactured. The gate carries a COLD control (2 waves after the timed frames) so deleting the warm-up goes red instead of both halves passing vacuously. **Measured 6.61 / 7.11 ms at 1920×1080, 0.9–1.0× median, 18th of 22** — its sidecar's unverified `complexity_cost.tier1 = 6.0` was close. **Coverage 21 of 28** (Arachne removed at D-246); the remaining six are Aurora Veil, Ferrofluid Ocean, Membrane, Murmuration, Nimbus, Staged Sandbox — `feedback` ×3 is still the cheapest next increment. |
| **Real-spectrum injection for the `direct` render path** | Supported (PR.19) | `MultiPassRenderHarness.realSpectrum` (nil = unchanged LCG fill); producer `NebulaSpectrumDiagnosticTests` via the production `FFTProcessor` over a session's `raw_tap.wav` | ★★ **EVERY AUTOMATED STILL OF A `direct` PRESET HAD BEEN RENDERED AGAINST NOISE.** `renderDirectPreset` fills the FFT buffer from a fixed LCG — the right choice for the frame-budget gate (deterministic, dense, no early-outs) and the wrong one for any artifact a human looks at. All four `direct` presets (Nebula, Plasma, Spectral Cartograph, Waveform) read the spectrum as their PRIMARY driver, three carry sync complaints in the 2026-09-04 roster review, and every artifact available to check them against was broadband noise — so a preset could be measured, gated and reviewed without anyone ever seeing what it draws on music. ★ **Pair the spectrum with REAL `FeatureVector`s too** (`SessionReplayHarness.loadRowsForReplay` on the same session's `features.csv`): driving synthetic ones leaves every deviation primitive at zero, which pins any D-026-coupled preset at its quietest state and invites reading that as its normal one. ★ The 512-bin buffer is written per frame from the harness's own pointer, so no binding changes and `PresetFrameBudgetTests` is untouched with the override unset. **Still missing:** QG.1 has no primitive for the spectrum, so the ring's shape — the subject of three of these four presets — cannot be expressed as a declared `audio_route` and nothing gates it. |
| **Per-track scalar reaching a `direct` preset** | Supported (PR.20) | `FeatureVector.trackHueAnchor01` / `track_hue_anchor01` (float 54, reclaimed from `_pad54`); `MIRPipeline.setTrackHueAnchor`; published from `VisualizerEngine.resetStemPipeline`; first consumer `Nebula.metal` | ★★ **BEFORE THIS, NOTHING TRACK-SCOPED COULD REACH A `direct` PRESET AT ALL.** Per-track variation existed (`lumenTrackSeedHash`) but arrives through per-preset STATE BUFFERS, which `direct` presets do not have — so Nebula, Plasma, Waveform and Spectral Cartograph could not vary by song in any way. ★ **Two stateless derivations were measured FALSE before touching the contract:** `accumulated_audio_time - track_elapsed_s` looks like the time at track start and is not — the clocks advance at ~1:11 — and no other field is constant within a track while varying between them. ★ **Reclaim a `_padNN`, never insert**: ORDER IS THE CONTRACT and the struct size must not move (same move as DYN.1b/DYN.2 on 51-52, D-158 on `_pad7`). ★★ **THERE ARE TWO MSL DEFINITION SITES** — `PresetLoader+Preamble.swift` AND `Renderer/Shaders/Common.metal` — and updating one leaves the other silently disagreeing with Swift; `CommonLayoutTest.mslFeatureVector_matchesSwiftSizeAndAgreesAcrossBothSites` is the gate and it caught exactly that here. ★ **Clear it on the complementary path** (`setTrackHueAnchor(0)` when identity is nil) or the previous track's value leaks across the boundary — CLAUDE.md §What NOT To Do in stored-property form. ★ A route on it is **`structural`, never `continuous`**: it is constant within a track by construction, so the continuous floor (the primitive must vary) would be exactly wrong. QG.1's fixtures predate the column, so it is listed in `columnsPostdatingFixtures` and the gate prints a FIXTURE GAP each run rather than passing silently — re-capturing would not help, since the property that matters is cross-track and is unit-tested instead. |\n| **Marschner-lite fiber material on a shipped preset** | Supported (PR.18) | `mat_silk_thread` (`Utilities/Materials/Organic.metal`) + `fiber_trt_lobe` (`Utilities/PBR/Fiber.metal`), first consumer `Gossamer.metal` | The V.3 cookbook's silk recipe and the V.1 fiber lobes shipped with the utility tree and **no preset called either for the whole of their existence** — Gossamer's own reference README specified them, with coefficients, and its shader drew constant-albedo strokes. ★ **The lobes are a HIGHLIGHT, not the light.** `azimuthal_r = 0.08` is a very narrow cuticle cone by design, so `m.emission` is ~0 off-highlight; returning it alone left the web ten times dimmer than the version it replaced. The body must be a separate wrapped-lambert term and the lobes ride on top. ★ **A planar web kills the TT lobe identically**: `dot(T, V) == 0` for every strand when every tangent lies in the view plane, so the back-lit warmth never fires anywhere — the fiber tangent needs an out-of-plane tilt, which real webs have anyway. ★ **`mat_silk_thread` never reads `p.absorption`**, so a recipe coefficient the reference set specifies has nowhere to land; either the recipe grows the term or the annotation is wrong. ★ The **cylindrical cross-section normal** — `normalize(float3(perp * u, sqrt(1 - u^2)))` from the signed offset across the thread — is what actually makes a line read as a round filament, and it is analytic, needing no normal map. Cost: the two `gossamerSilk` calls ablated at under 0.6 ms of Gossamer's 3.4 ms increase. |
| **Transient-aligned accent primitive** | Supported (PR.22) | `FeatureVector.transientRise` / `transient_rise` (float 55); `SpectralAnalyzer.advanceTransientRise`; first consumer `Nebula.metal` | ★★ **AN ACCENT KEYED TO THE MOST EVENT-SHAPED PRIMITIVE IS NOT THE SAME AS ONE KEYED TO THE FASTEST.** `spectral_level_rise` is a fixed-lag difference (`level(t) − level(t−0.15)`), so it stays elevated until the lagged term catches up and its PEAK lands ~150 ms after the transient — by construction, not as a defect. A preset that picks it for an accent inherits that. `transientRise` is the same statistic at 15/40 ms: **+30 ms** measured against offline onset strength. ★ **CROSS-CORRELATION CANNOT CALIBRATE A THRESHOLD** — it is scale-invariant, so a detector that never fires still reports a lag. The short window at the parent's 2–7 dB band fired ~3× as often on real audio (1.87/s vs 0.53/s); 4–10 dB matches the parent's rate while keeping the timing. Match a DISTRIBUTION on real material, per `SpectralAnalyzer+Density`'s own rule. ★ **SYNTHETIC PROBES MEASURE THE WRONG THING HERE**: a step saturates the dB band at any window (33 ms apparent lead), a 250 ms ramp never leaves the floor, a brief burst is dominated by the shared release (17 ms) — only a SUSTAINED step separates the windows (150 ms). ★ A sibling field, not a retune: `levelRise` has consumers (FTR.24). ⚠ Degrades to one frame of lag at the 10 Hz local-file rate (BUG-087) — never worse than the parent, better only where the analysis rate is high. |\n| Certification rubric ladder (mandatory / expected / preferred) | Supported | V.6 `FidelityRubric` + `PresetDescriptor.certified` | Uncertified presets excluded from Orchestrator by default (D-067). |
| Photosensitivity flash-safety gate (Harding / WCAG 2.3.1, ≤ 3 flashes/s) | Supported (15/15) | CLEAN.7.6 / D-164 + CLEAN.7.6b (Nimbus) + CLEAN.7.6c (multi-pass): `FlashAnalyzer.swift` (`Sources/Renderer`) + `PhotosensitivityCertificationTests` (single-pass) + `MultiPassFlashHarnessTests` + `RenderPipeline+MVWarpHeadless.swift` | Renders each certified preset over a worst-case beat + stem train and fails cert at > 3 full-frame flashes/s. **All 15 certified presets measured SAFE (≤ 1 flash/s):** FFO / Murmuration / Nimbus / **Aurora Veil** (AV.7) single-pass (FeatureVector in-shader + the ticked Nimbus follower); **Lumen Mosaic** (rayMarch G-buffer + post + 4-light follower @ the live 128-step budget), **Dragon Bloom / Fata Morgana / Skein / Nacre / Floret / Glaze** (mv_warp FEEDBACK — **not** rayMarch; CLEAN.7.6/7.6b mislabelled DB+FM as rayMarch, their passes are `["direct","mv_warp"]`; Nacre/Floret/Glaze are the bespoke `renderNacre`/`renderFloret`/`renderGlaze` branches — NACRE.4 / FLORET.4 / GLAZE.8), and the **particle geometry-driven cell colonies Filigree** (`PhysarumGeometry` trail, PHYS.5) / **Mitosis** (`MitosisGeometry` RD field, MITOSIS.2c) / **Cytokinesis** (`MitosisGen2Geometry` explicit cells, `renderCytokinesis` — MITOSIS-G2.3, 0.00 flashes/s) / **Cymatic Resonance** (`CymaticSandGeometry` vibrating sand, `renderCymaticSand` — CR.2/D-199, **0.00 flashes/s** — sand is conserved so global luminance is steady) driven headless through the real `renderMVWarpToTexture` / `renderFataMorgana(target:)` / `renderGlaze` / `renderFiligree` / `renderMitosis` / `renderCytokinesis` / `renderCymaticSand` seams with feedback persistence. Static renders are still never asserted safe (fail-loud `responded` guard). **The cert gate is the photosensitivity enforcement mechanism: the output-side runtime clamp (CLEAN.7.6d) was evaluated and NOT pursued (D-166)** — no single pipeline chokepoint (8 present paths), all shipped presets ≤ 1 flash/s, `RayMarchPipeline:94` OR-flag slot reserved. Residual **A-next** (open): regional-area / saturated-red-flash channels. |
| Per-preset audio route-coverage gate (declared routes must fire on real music) | Supported (QG.1, D-180) | `PresetDescriptor.audioRoutes` (sidecar `audio_routes`) + `AudioRoutePrimitives` (`Sources/Presets`) + `RouteCoverageTests` / `AudioRouteSchemaTests` (`Tests/…/Presets`) + `FidelityRubricTests.certifiedPresetsDeclareAudioRoutes` + `Fixtures/route_coverage/` | Every preset declares its routes (`{route, primitive, kind}`); the gate replays the canonical real-audio fixtures and asserts each declared primitive fires per its kind's floor (continuous non-constant / accent ≥1 rising crossing / structural ≥1 boundary). **157 routes / 14 presets, un-gated** (count as of TONAL.3 — the live number is `RouteCoverageTests` output, not this cell) (always enforced; Ricercar's FL.13 flow-field backfilled +7 as a BUG-067 follow-up, +4 family-capture `*ActivityDev` reads armed + green at QG.1.3 — RouteCoverageTests 156/14, 0 red). Certification requires a non-empty manifest whose routes are all green — mechanizes the per-route firing evidence that was a prose closeout obligation (`vocalsPitchConfidence` 0 % for 5 months). **A red route is the gate working — file it, never tune the floor.** **Coverage boundary (QG.1.1):** the offline `StemAnalyzer` fixture can't populate VisualizerEngine-CPU-computed `StemFeatures` fields (FFO spike/aurora, Dragon Bloom tumble clock) → documented, not declared; live-session + sparse/bright fixtures are the follow-up. Ricercar's 4 family-capture `*ActivityDev` reads were an instance of this (offline generator ran `StemAnalyzer` only) — armed at QG.1.3 by extending `FixtureSessionCaptureGenerator` to also run the offline `InstrumentFamilyAnalyzer` PANN sweep (Layer-5a, sampled by playback position) and merge the per-frame family activity into the stems rows; PANN jitter clears the `continuous` floor on the checked-in clips (KNOWN_ISSUES §QG.1.1). |
| Render-pipeline-state cache keyed by full compiled identity | Supported (CLEAN.4.4) | `ShaderLibrary.renderPipelineState(...)` — `PipelineKey(name, pixelFormat.rawValue, supportICB)` | The descriptor varies by color-attachment-0 `pixelFormat` and `supportIndirectCommandBuffers`; a name-only key would return the first-compiled PSO for any later call sharing the name (wrong format / ICB capability). All current production callers use unique names, so this was **latent hardening, not a live bug** (preset multi-pass PSOs bypass the cache via `PresetLoader`; `supportICB: true` is test-only). Gated by `ShaderLibraryTests` (same name + different format/ICB ⇒ distinct PSOs). |
| **Marched-pixel budget for ray-march presets (resolution-aware `render_scale`)** | Supported (PERF.14) | `RenderPipeline.marchScale(declared:width:height:)` + `marchedPixelBudget` (`RenderPipeline+RayMarch.swift`); gate `RayMarchScaleBudgetTests` | Caps **marched** pixels at 1536×864 instead of scaling the drawable by a constant, because ray-march cost here is a **STEP, not a curve**: VL at `render_scale` 0.5 measured **175 ms** at 3840×2160 (1920×1080 marched) and **≤ 15 ms** at 0.4 (1536×864 marched) — an **11.7× drop from a 1.56× pixel cut**, live, same build, same preset. ★★ **"ms per megapixel" does not model this** — PERF.11→13 all reasoned with it and all mispredicted; the offline harness never crosses the step, which is why it projected 0.4 would buy ~1.25× when live it bought ≥ 11×. The budget only ever LOWERS a scale and applies only to presets that **declare** `render_scale`, so the full-resolution catalog keeps its certified looks; VL's sidecar stays 0.5 and resolves to 0.5 at 1080p, 0.5 in the 2884×1662 window it was certified in, and 0.4 at 4K. ⚠ **`render_scale` has a silent 0.4 floor** — probes at 0.25 and 0.35 both ran at 0.4 and returned identical timings (p5 15.29/15.29) before it was noticed. |
| Per-pass GPU timestamp / breakdown for governor decisions | Missing | Frame-level only | Would help diagnose which pass is breaching budget when staged compositing lands. **Not what unblocked VL (PERF.14)** — the cliff was found by moving one number and measuring live, not by decomposing the frame. ⚠ When it does land, note that `frame_gpu_ms` (`cb.gpuEndTime - cb.gpuStartTime`) **includes the wait on the display**: it floors at ~15.3 ms at 60 Hz (0 frames of 11,000 below 15.0 across two sessions), so it cannot price headroom below one refresh. |
| p95 / p99 frame-time tracking surfaced to certification | Partial | `FrameTimingReporter` exposes percentiles; rubric M6 reads `complexity_cost`, not measured p95 | Closing this means the governor's data feeds rubric M6 directly. |

---

## Preset implications

The capability gaps above shape what preset families are buildable today, what is buildable with localised additions, and what is blocked.

### Buildable today (no engine work)

- **Direct-fragment 2D SDF presets that read FeatureVector + StemFeatures + SpectralHistory** (instrument-family diagnostics like Spectral Cartograph; minimal abstract presets like Plasma, Waveform, Nebula).
- **Direct-fragment + mv_warp presets** that need temporal feedback accumulation but no per-pass compositing (Volumetric Lithograph, Starburst, Gossamer's current architecture, **Dragon Bloom Spike 1** — first preset using `passes: ["direct", "mv_warp"]` explicitly, polar-waveform brush accumulated through the warp + decay chain; D-135). This includes the **canvas-hold** configuration (identity warp + no-decay + no-transfer = lossless paint accumulation), proven **config-only — no engine work** at Skein.ENGINE.1 (D-142); the four properties are reachable purely through the preset's own `mvWarpPerVertex`/`mvWarpPerFrame` + the default `mvWarpChromatic=0`. **Marks-on-top canvas-hold presets** (a custom overlay painting onto a per-preset coloured ground) are fully buildable since **Skein.ENGINE.1.1 (D-143)** — declare `<prefix>_geometry_*` + a `marks` descriptor block (draw params + `chromatic` + `comp` + `beat_pulse` + `canvas_clear`); the marks-on-top mechanism is no longer Dragon-Bloom-specific. First consumer: Skein (the wandering painter + pour line **landed at Skein.1**, 2026-06-05 `57ee7383` — path A, a closed-form `f(features.time)` trajectory computed in the overlay **vertex** shader and passed to the fragment as varyings; **no engine touch, no per-preset buffer, no CPU state**. **Skein.2 splatter morphology also landed path A** (2026-06-05 — hash-seeded ragged-edge droplet bursts + filaments + a debug viscosity sweep, all closed-form in `skein_geometry_fragment`; still no engine touch). **ENGINE.1.2** (the gated slot-6 overlay-buffer binding) landed with **Skein.3** (D-147) — `SkeinState`'s per-stem flow integrators + onset-burst ring + per-track SHA seed reach the overlay fragment at `buffer(6)`. **ENGINE.2** (D-149) adds a **canvas-hold wetness channel** (transient freshness in the feedback ALPHA — stamp-where-painted + decay-pauses-at-silence; the row above), enabling **Skein.4's wet/dry sheen** (`skein_comp_fragment` — GGX specular gated by wetness, normal from the canvas luminance gradient, dry→matte; the wet-now/dry-past legibility device). Both are gated/byte-identical and touch **no shared GPU code** (the per-prefix `<prefix>_warp/comp_fragment` override). So the full canvas-hold painterly stack — lossless accumulation + marks-on-top + per-stem colour + a read-time wet/dry sheen — is buildable today with no engine paradigm work.)
- **Ray-march scene presets** with deferred PBR + IBL + SSGI + post-process (Volumetric Lithograph, Test Sphere). These already get fog, IBL ambient, hero specular, optional SSGI for free. (Glass Brutalist, the original SSGI consumer, was retired GBRETIRE.1 / D-186; Kinetic Sculpture retired KSRETIRE.1 / D-188.)
- **Mesh-shader / particle / ICB presets** for any concept where the visual element is geometry-instanced (Fractal Tree; future murmuration variants). **Caveat CLEARED (FTR.4, 2026-08-11):** `StemFeatures` is bound at object/mesh buffer(3), so mesh concepts may route per-stem and instrument-family audio. Since FTR.10 a beat-held `FeatureVector` is also bound at object/mesh buffer(4), so a mesh preset can make any layer step on the cached beat grid without CPU state of its own.
- **Compute-driven agent-network presets** (physarum / slime-mold, trail-following swarms, agent-deposit fields) — demonstrated buildable today on existing paths (per-frame compute + `atomic_uint` deposit accumulator + ping-pong `r16Float` trail + particle-mode draw), no engine work. First consumer: **Filigree** — **CERTIFIED 2026-06-28** (PHYS.5; a loose energy-accompaniment: energy merges/divides the cells, but the trail substrate can't carry tight beat-sync — tightly-synced cell division is the separate reaction-diffusion preset, now built: **Mitosis**, below).
- **Compute-driven reaction–diffusion presets** (Gray–Scott cell colonies, Turing patterns) — built today on the same path as the agent-network row, simpler (multi-channel `rg16Float` ping-pong + N react substeps/frame, no agents/accumulator), no engine work. First consumer: **Mitosis** — **CERTIFIED 2026-06-29** (MITOSIS.2c, "psychedelic cell division"): a slow growth→crowd→dissolve→regrow cell-division cycle, fluorescence-microscopy colour (magenta nuclei + cyan/green B-gradient membranes), music-tied hue; flash-safe (0.00/s, `renderMitosis`). The growth TRANSIENT is the division (constant-param GS freezes to a static attractor — that's the *crowded* end-state, by design, then it dissolves & regrows).

### Buildable today with localised additions

- **Volumetric / participating-media-heavy presets** (cloudscapes, fog rooms). Volume utilities exist (`Utilities/Volume/*`); a preset composes them in-fragment without engine changes (Nimbus NB.1 is the first), but lacks depth so anything depth-modulated is approximate. **Cost rule (proven NB.1, reinforced NB.2, 2026-06-04):** do NOT compute `fbm`/`voronoi` per march step — that's ~20 ms @1080p, over the 7 ms Tier-2 ceiling. Sample the precomputed `noiseVolume` 64³ 3D texture per step instead (production-bound on the direct path) → ~1.4 ms @1080p. **The cost is the per-step ALU, not the sample count:** NB.2's full meso/micro cascade doubled the taps (3→6/step) for only +0.28 ms (→1.65 ms), because the body early-out keeps most steps free — add octaves freely *as texture taps*. Use true-3D `noiseVolume`, not a 2D texture (columnar artifacts in a 3D march). Half-res march + MetalFX (NB.8) is then a headroom reserve, not a requirement (NIMBUS_DESIGN §6.1/§6.2). Detail-cascade recipe: SHADER_CRAFT §6.5.
- **Caustics, voronoi-based texture presets**. Utilities are in place (V.2 Texture tree). (Reaction-diffusion is now *built* — see Mitosis under "Buildable today on existing paths" above.)
- **Atmosphere-utility-promoted presets** (sky band, dust motes, light-shaft helpers). Lifting Arachne's atmosphere into shared utilities would unblock similar treatments in future presets at low cost.

### Unblocked by V.ENGINE.1

- **Per-preset multipass with named offscreen textures and pass-separated harness capture.** The `RenderPass.staged` scaffold (linear DAG, samples bound at `[[texture(13)]]`+) plus the `RENDER_VISUAL=1` per-stage harness landed in V.ENGINE.1. `StagedSandbox` is the live diagnostic. **V.7.7A (2026-05-05) migrated Arachne onto this path** with placeholder WORLD + COMPOSITE stages; full WORLD detail, refractive droplets, full silk geometry, and the spider arrive in V.7.7B+. Refractive 2D droplets sampling a previously-rendered WORLD texture, depth-of-focus on a single named layer, and any future "small foreground elements refract the scene behind them" preset (rain on a window, oil-on-water, wet fabric, aquarium glass) can now be authored without further renderer changes.

### Still blocked or severely limited until renderer changes land

- **Anchor-position metadata buffer from one stage consumed by the next.** V.ENGINE.1 plumbs textures across stages but not arbitrary uniform buffers. Arachne v8 needs the WORLD pass to publish 4–7 branch-attachment positions for the WEB pass to terminate radials on. A small follow-up (named per-stage `MTLBuffer` outputs in addition to texture outputs) closes this; until then the WEB stage must derive anchor positions from the same `rng_seed` the WORLD stage uses.
- **Depth attachment for 2D staged presets.** Stages output color only. True depth-modulated fog and depth-aware DoF still require either a per-stage `.r16Float` depth target or a synthesised depth buffer. Approximate DoF (per-stage uniform blur intensity) is authorable today.
- **Whole-scene shake / vibration on bass impulses** (spider footstep; dub-bass room shake). No engine-level scene-shake primitive; would currently require preset-side feedback-warp tricks that don't compose cleanly with mv_warp.
- **Forest-stage / nature-scene presets that re-use a layered backdrop** (Arachne v8, future Gossamer "in the world" revisit, future Stalker forest variant). The first WORLD pass is bespoke; without promotion to a shared module, every nature-scene preset re-pays the implementation cost.
- **Light-shaft-as-composited-pass presets** (cathedral interior; sun through trees; dance-floor smoke beams). Volume utilities exist, but no compositing pass to apply shafts on top of the previously-rendered scene.
- **Presets that need to combine 3D ray-march with 2D direct-fragment overlay** (a ray-marched object inside a 2D illustrated frame). Not currently a documented combination.
- **Per-pass anti-reference rejection** for any preset where a known wrong-output JPEG should hard-fail certification. Affects Arachne (refs 09, 10), and would apply to any future preset with anti-references.
- ~~**Headless multi-pass / state-reconstructing render for offline analysis** (QG.3 blocker)~~ — **RESOLVED at QG.3.1 (D-183).** The photosensitivity flash gate's real per-preset render seams (`render*ToTexture` + ticked followers + particle geometries, feedback persistence) were extracted into the shared `MultiPassRenderHarness`, and the coupling report now drives them with the real reconstructed-fixture train — measurable for all 13 certified presets (was 2). Any future offline analysis that needs the animated multi-pass output (not just the coupling report) can reuse this harness. Remaining approximation: the 3 single-pass presets render one fragment (+ Nimbus follower), and Ferrofluid Ocean's post/height-field passes are not yet in the harness (single-fragment-approximated) — a bounded follow-up, not a blocker.

### Implication for the implementation queue

V.ENGINE.1 (2026-05-05) landed the highest-leverage engine investment: per-preset multipass orchestration with named offscreen textures and a pass-separated harness hook. V.7.7A (2026-05-05) migrated Arachne onto the scaffold with placeholder WORLD + COMPOSITE stages — V.7.7B+ now layers the real WORLD detail, refractive droplets, full silk geometry, and spider on top of the staged shape. Any future preset family that needs cross-pass texture sampling is unblocked.

The remaining engine investments, in priority order:

1. **Per-stage uniform-buffer outputs** (small follow-up). Lets the WORLD pass publish anchor positions for the WEB pass to read. Closes the last "Missing" entry in §2 and §4 of this registry that Arachne v8 specifically depends on.
2. **Per-stage `.r16Float` depth attachment** for 2D staged presets. Unblocks true depth-modulated fog and depth-aware DoF.
3. **Whole-scene shake primitive** at the renderer level, distinct from preset-side feedback warp. Spider footstep / dub-bass room shake.
4. **Per-pass GPU timing attribution** for the budget governor. Frame-level only today; per-stage cost data would let the governor scale the most expensive stage instead of dropping the whole pass.
5. **Anti-reference rejection gate** in the certification harness — automated dHash comparison against known anti-reference JPEGs (refs 09 / 10 for Arachne).
6. **Atmosphere-utility promotion** (sky band, dust motes, light-shaft helpers) into shared utilities once Arachne's WORLD stage is authored, so future "in-the-world" presets reuse rather than re-author.
7. **Harness PNG-export bug for staged presets** (small follow-up). `PresetVisualReviewTests.makeBGRAPipeline` currently calls `Bundle.module.url(forResource: "Shaders")` from the test target's bundle, where `Shaders` is not a resource — throws `cgImageFailed` for any staged preset under `RENDER_VISUAL=1`. Fix: source the `.metal` file via `Bundle(for: PresetLoader.self)`. Required before V.7.7B's harness contact-sheet review since per-stage PNGs are how WORLD-only / WEB-only / COMPOSITE outputs are inspected during authoring.
