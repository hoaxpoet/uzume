# Preset Session-Start Checklist

Run this at the **start** of any preset-related increment — authoring, uplift, tuning, or fix — before opening a `.metal` file. It replaces the former Failed Approaches #39 and #63 and the Arachne read-first bullet (RB.2, 2026-06-11); Part 2 holds the preset-session discipline rules moved from CLAUDE.md §Authoring Discipline (RB.2-2). The reasoning: Claude Code has no visual feedback while writing shader code, so anchoring on the curated references and the preset's design history is the only reliable path to quality — and the transcript record shows prose rules alone did not make that happen (README-before-first-edit measured at 35 % compliance, REVIEW.1). A checklist at session start is the mechanism.

## Part 1 — Session-start steps

1. **Read `docs/VISUAL_REFERENCES/<preset>/README.md` cover to cover.** The README carries per-image trait-trustability annotations and disregard-this-property warnings that prompt text won't have. If no curated reference set exists for the preset: curate it first as part of the session, or escalate to Matt before authoring.
2. **Read each reference image's annotation.** Note the mandatory-traits checklist and the anti-references (what the preset must NOT look like).
3. **Read the preset's design doc if one exists** (`docs/presets/ARACHNE_V8_DESIGN.md`, `LUMEN_MOSAIC_DESIGN.md`, `SKEIN_DESIGN.md`, …). Per-preset operating rules and tuning history live there, not in CLAUDE.md.
4. **Cite specific reference filenames** in design comments wherever they motivate a design choice.
5. **Mid-session sanity checks are side-by-side comparisons against the named reference images** — never a self-judgment of "looks reasonable." **Mechanized (D-181):** before EVERY tuning commit, run `Scripts/compare_render.sh <preset>` (composites the newest `RENDER_VISUAL=1` frames against `docs/VISUAL_REFERENCES/<preset>/` into one sheet), **Read** the printed sheet, and write a **verdict table** into the transcript — one row per mandatory trait from the reference README: `trait | reference filename | PASS/FAIL | what differs (one sentence)`. Anti-reference rows are mandatory (`does NOT resemble <anti>` is a row). A FAIL must change the next action; committing with an unexplained FAIL requires stating why in the commit body. The sheet reader is your eyes — no automated similarity score substitutes (D-064).
6. **Render early.** Produce a `RENDER_VISUAL=1` contact sheet before the first tuning commit. The heaviest historical preset sessions burned 85 %+ of their output tokens before any rendered evidence existed (REVIEW.1 §1.3); rendered evidence early is what ends tuning spirals.
7. **Motion is not stills — gate it before M7 (the Truchet hole).** Steps 5–6 composite *stills*; temporal defects (jitter, structure-pop, strobe, freeze) are invisible in a still sheet — Truchet Loom passed still-review and jittered "like a bug" in live M7 (TLRETIRE.1 / D-194). Before requesting M7, render a contiguous frame **sequence** (not the 3-still contact sheet) and run `Scripts/motion_gate.sh <preset> <frames-src>`. It computes a frame-to-frame motion signal (spike count = jitter/pop; ~0 = freeze), stages sampled frames, and points at `target_animated.gif`. **Read** the sampled frames as a sequence and write a **motion verdict** into the transcript: smooth? on-concept *in motion*? matches the reference's temporal character? A spike-laden or frozen signal must change the next action — never request M7 on unreviewed motion. Reader is your eyes (D-064); the spike count is evidence, not an auto-pass. (The sequence feed reuses `renderFrame` in `PresetVisualReviewTests`; existing certified presets regression-check today against their committed `target_animated.gif`.)

## Part 2 — Preset-session discipline (from CLAUDE.md §Authoring Discipline, RB.2-2)

**Articulate the musical role before authoring anything.** Before the JSON sidecar, before the reference images, before any `.metal` edit, write a one-sentence answer to: *"How is this preset's primary visual subject another instrument in the band?"* The sentence must name a specific musical feature (a beat, a downbeat, a sustained bass envelope, a vocal pitch contour, a structural boundary, a build-up) and a specific visual behaviour the listener will pair with it. "Vibe with the music" / "reactive to energy" are not answers. If you cannot produce the sentence, stop and bring the gap to Matt. The Drift Motes concept failed this gate and four days of iteration did not recover.

**Reference images are still moments; presets are behaviours over time.** A photograph shows what the *frame* should resemble; it does not encode the *temporal contract* — what changes when the bass drops, what fires on a downbeat, what carries the listener through a 30-second cycle. Pair every reference image with an explicit temporal contract before the first commit.

**Three-part bar for any new preset concept.** Every concept must clear all three; pitches that pass two of three and rely on Matt to spot the missing third are not acceptable — surface concerns before the pitch:
1. **Iconic visual subject deliverable at fidelity** — demonstrable from a comparable past preset. If Matt has flagged a fidelity gap on a similar preset before, the default is "I cannot deliver this" until proven otherwise.
2. **Clear musical role** — the one-sentence rule above.
3. **Infrastructure-feasible** — no render passes / engine surfaces / GPU contracts Uzume lacks; if infrastructure must be added, confirm with Matt it's worth the increment.

**After every M7 round, write one sentence: "what I now believe about why this preset is failing."** If the sentence doesn't change between rounds, the iteration is mechanical and the next increment is wasted — stop and re-scope.

**Test in the production-grade rendering pipeline. No shortcuts.** Every preset increment that depends on temporal behaviour must include a test running the same dispatch path the live app uses — for ≥ N frames covering the relevant accumulator decay window — and inspect the multi-frame output. "Same dispatch path" means: `mv_warp` → scene → warp → compose → swap in a loop; `staged` → all stages with the same slot-6/slot-7 bindings production uses; `ray_march` → G-buffer → lighting → composite. Tests that hit `preset.pipelineState` alone verify instantaneous output only — three Aurora Veil increments shipped green that way while smearing in live playback. Obligations:
1. Before authoring a preset using `mv_warp` / `staged` / `feedback` / `ray_march` + `post_process`, write or extend the multi-frame harness FIRST — verify the live path is reachable from a test before any shader work.
2. Every paradigm has a named reference template to copy-adapt (QG.4, D-182) — env-gated `HARNESS_TEMPLATES=1`, 60 frames at silence, capture + metric, all built on the shared `HarnessTemplateCore` spine. Adapt the one matching your paradigm; do not reinvent:
   - `mv_warp` → `AuroraVeilMVWarpAccumulationTest` (scene → warp → compose → swap; accumulator capture + quantitative metric).
   - `staged` → `StagedPathHarnessTemplate` (all stages via the production `encodeStage`, slot-6/7 bindings; per-stage non-degenerate + composite dHash golden).
   - `ray_march` → `RayMarchPathHarnessTemplate` (live `RayMarchPipeline.render`: G-buffer → lighting → composite → post; non-degenerate + composite dHash golden).
   - `feedback` → `FeedbackPathHarnessTemplate` (surface-mode `runWarpPass` → composite → swap; accumulator bounded between the D-037 non-black floor and saturation).
3. Single-frame tests stay valuable for shader-math gates; they are not sufficient alone.
4. Closeout reports state which dispatch path the tests exercised — "tests pass" alone is not evidence of correctness. Tell-tale bypass phrasing: "the silence test passes, so silence is stable" / "the visual review PNG looks clean."

**Design is upstream of testing — surface grounding risks before code.** Required grounding per mechanism, in descending preference: (1) working code reference in a comparable visual context (cite the Shadertoy ID / repo / demo URL); (2) academic paper or physics derivation with implementable math; (3) nothing but the design doc's assertion — highest risk, surface explicitly as "no empirical grounding for X" and let Matt accept or descope. A *combination* of two mechanisms needs grounding for the combination itself, not just each piece (Aurora Veil's failure: mv_warp layered on nimitz's recipe, a pairing no published demo used). Reaching level 3 on any non-trivial mechanism = surface immediately.

**Evidence-based closeouts — diagnostic infrastructure precedes fidelity claims.** A closeout asserting an audio-coupled route works must cite per-route firing evidence from the session's `features.csv` / `stems.csv` (frame counts, threshold-crossing percentages, video-frame extracts at the audio events). As of QG.1 (D-179) this evidence is **mechanized**: every preset declares its routes in the `audio_routes` sidecar manifest (SHADER_CRAFT §17.1), and `RouteCoverageTests` replays the canonical fixture set to assert each declared primitive fires per its kind's floor — certification requires a non-empty manifest (`FidelityRubricTests.certifiedPresetsDeclareAudioRoutes`) whose routes are all green. A **red route is the gate working**: file it in `KNOWN_ISSUES.md` as a route defect; never tune a floor to make it pass. The manifest is the standing form of "the route works" — a closeout adds a route only after confirming the code reads it (audit before declaring). A closeout asserting visual-family membership must cite rubric proxy scores + reference centroid + σ-distance. The tool is `PresetSessionReplay` (see `docs/ENGINE/SESSION_REPLAY.md`). When the diagnostic for a question doesn't exist, the closeout says **"cannot verify X"** — and building the missing diagnostic is the next increment, not a future task (`vocalsPitchConfidence` sat at 0 % for ~5 months while closeouts claimed the route worked). Verdicts on broken proxies are forbidden: `uncalibrated` means the proxy is unreliable, not that the render is OK — refine the proxy or say "cannot verify."

**Chain health precedes fidelity claims (ASH.2 / D-184).** Every M7-bound session and every reel recording must be captured on a healthy audio chain — a fidelity judgment made on degraded audio measures the wrong thing. Each finished session dir carries a machine-written `chain_health.json` + a `CHAIN_HEALTH:` line (written by `ChainAnalyzer` at session end; regrade any dir with `Scripts/analyze_session_chain.sh <dir>`). **A fidelity/M7 closeout must cite the session's `verdict`; a non-`clean` verdict (`degraded`/`broken`) must be explicitly flagged** — do not fold a fidelity conclusion off a `degraded` capture without saying so and re-capturing. (Note the onset-count field is informational only — AGC-invariant, not a chain-health signal; the verdict is driven by peak + dead-tap + DRM.)

**Silence is not one look — it is a per-preset design decision (Matt, 2026-09-12, BUG130.1 M7).** *"Silence will read as different things depending on the preset's design."* Do not assume a silence gate means "arrest all motion", and do not file continuing motion at zero energy as a defect before asking what the preset's substrate does without drive. Alfvén is the worked example: a driven MHD field whose `alf_force` scales entirely by `p.drive`, so at silence the stirring force stops injecting structure while the field it already holds keeps advecting and diffusing. It **coasts** — motion continues, complexity drops — and Matt's call was that coasting is right there, because a hard freeze on a fluid sim reads as a dropped frame rather than as quiet. A particle or event-driven preset may legitimately want the opposite. So: state in the preset's design doc what silence should LOOK like for that substrate, and make the M7 judge it against that, not against a global stillness bar. ⚠ This question was unreachable on the local-file path until BUG-130 was fixed (the feature vector never reached silence there), so silence behaviour recorded before 2026-09-12 on that path was never actually exercised.
